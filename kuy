-- Executor Hub v12.1 - Complete GUI (มุมเหลี่ยม ไม่โค้งเลย)
-- ลบความโค้งมุมทั้งหมด + ลบตัวเลือกปรับมุมใน Settings

local Players = game:GetService("Players")
local LocalPlayer = Players.LocalPlayer
-- INSERT: รอ LocalPlayer ให้พร้อม
if not LocalPlayer then
    repeat task.wait() LocalPlayer = Players.LocalPlayer until LocalPlayer
end
local TweenService = game:GetService("TweenService")
local UserInputService = game:GetService("UserInputService")

local sg = Instance.new("ScreenGui")
sg.Name = "HubFinalNoCorner"
sg.ResetOnSpawn = false
sg.Parent = LocalPlayer:WaitForChild("PlayerGui")

-- Main Frame (มุมเหลี่ยม = 0)
local Main = Instance.new("Frame")
Main.Size = UDim2.new(0, 400, 0, 340)
Main.Position = UDim2.new(0.5, -200, 0.5, -170)
Main.BackgroundColor3 = Color3.fromRGB(20, 20, 28)
Main.BorderSizePixel = 0
Main.ClipsDescendants = true
Main.Visible = true
Main.Parent = sg

-- ไม่มี UICorner แล้ว → มุมเหลี่ยมตรง

local Stroke = Instance.new("UIStroke")
Stroke.Color = Color3.fromRGB(70, 70, 120)
Stroke.Transparency = 0.75
Stroke.Thickness = 1
Stroke.Parent = Main

-- Title Bar (มุมเหลี่ยม)
local Title = Instance.new("Frame")
Title.Size = UDim2.new(1, 0, 0, 36)
Title.BackgroundColor3 = Color3.fromRGB(28, 28, 38)
Title.BorderSizePixel = 0
Title.Parent = Main

local TitleText = Instance.new("TextLabel")
TitleText.Size = UDim2.new(1, -120, 1, 0)
TitleText.Position = UDim2.new(0, 14, 0, 0)
TitleText.BackgroundTransparency = 1
TitleText.Text = "YOnoOb Hub"
TitleText.TextColor3 = Color3.fromRGB(210, 210, 240)
TitleText.Font = Enum.Font.GothamSemibold
TitleText.TextSize = 16
TitleText.TextXAlignment = Enum.TextXAlignment.Left
TitleText.Parent = Title

-- Control Buttons (มุมกลมเดิมไว้ เพราะเป็นปุ่มเล็ก ๆ ดูดีกว่า)
local function createBtn(posX, color, hover, text)
    local btn = Instance.new("TextButton")
    btn.Size = UDim2.new(0, 28, 0, 28)
    btn.Position = UDim2.new(1, posX, 0, 4)
    btn.BackgroundColor3 = color
    btn.Text = text
    btn.TextColor3 = Color3.new(1,1,1)
    btn.Font = Enum.Font.GothamBold
    btn.TextSize = 14
    btn.BorderSizePixel = 0
    btn.Parent = Title

    local c = Instance.new("UICorner")
    c.CornerRadius = UDim.new(1, 0)  -- ปุ่มยังกลมอยู่
    c.Parent = btn

    btn.MouseEnter:Connect(function() TweenService:Create(btn, TweenInfo.new(0.15), {BackgroundColor3 = hover}):Play() end)
    btn.MouseLeave:Connect(function() TweenService:Create(btn, TweenInfo.new(0.15), {BackgroundColor3 = color}):Play() end)

    return btn
end

local MinBtn   = createBtn(-94, Color3.fromRGB(220, 160, 0), Color3.fromRGB(240, 180, 0), "-")
local MaxBtn   = createBtn(-62, Color3.fromRGB(0, 160, 100), Color3.fromRGB(0, 190, 120), "□")
local CloseBtn = createBtn(-30, Color3.fromRGB(200, 60, 60), Color3.fromRGB(230, 80, 80), "×")
-- INSERT: ให้ปุ่ม Close ซ่อน GUI เมื่อกด
CloseBtn.MouseButton1Click:Connect(function()
    Main.Visible = false
end)

-- Tab Bar (มุมเหลี่ยม)
local TabBar = Instance.new("Frame")
TabBar.Size = UDim2.new(1, 0, 0, 34)
TabBar.Position = UDim2.new(0, 0, 0, 36)
TabBar.BackgroundColor3 = Color3.fromRGB(25, 25, 35)
TabBar.BorderSizePixel = 0
TabBar.Parent = Main

-- Content
local Content = Instance.new("Frame")
Content.Size = UDim2.new(1, -16, 1, -80)
Content.Position = UDim2.new(0, 8, 0, 74)
Content.BackgroundTransparency = 1
Content.Parent = Main

-- Pages
local MainPage = Instance.new("ScrollingFrame")
MainPage.Size = UDim2.new(1,0,1,0)
MainPage.BackgroundTransparency = 1
MainPage.ScrollBarThickness = 4
MainPage.Visible = true
MainPage.Parent = Content

local SettingsPage = Instance.new("ScrollingFrame")
SettingsPage.Size = UDim2.new(1,0,1,0)
SettingsPage.BackgroundTransparency = 1
SettingsPage.ScrollBarThickness = 4
SettingsPage.Visible = false
SettingsPage.Parent = Content
MainPage.CanvasSize = UDim2.new(0, 0, 0, 2500)

local MainLayout = Instance.new("UIListLayout")
MainLayout.Padding = UDim.new(0, 10)
MainLayout.HorizontalAlignment = Enum.HorizontalAlignment.Center
MainLayout.Parent = MainPage

local SettingsLayout = Instance.new("UIListLayout")
SettingsLayout.Padding = UDim.new(0, 10)
SettingsLayout.HorizontalAlignment = Enum.HorizontalAlignment.Center
SettingsLayout.Parent = SettingsPage

-- Tab Buttons
local function createTab(name, page)
    local tab = Instance.new("TextButton")
    tab.Size = UDim2.new(0.5, -4, 1, -4)
    tab.BackgroundTransparency = 1
    tab.Text = name
    tab.TextColor3 = Color3.fromRGB(160, 160, 200)
    tab.Font = Enum.Font.GothamSemibold
    tab.TextSize = 15
    tab.Parent = TabBar

    tab.MouseButton1Click:Connect(function()
        MainPage.Visible = (page == MainPage)
        SettingsPage.Visible = (page == SettingsPage)
        TweenService:Create(tab, TweenInfo.new(0.2), {TextColor3 = Color3.fromRGB(220, 220, 255)}):Play()
        for _, other in ipairs(TabBar:GetChildren()) do
            if other:IsA("TextButton") and other ~= tab then
                TweenService:Create(other, TweenInfo.new(0.2), {TextColor3 = Color3.fromRGB(160, 160, 200)}):Play()
            end
        end
    end)

    return tab
end

local TabMain = createTab("Main", MainPage)
TabMain.Position = UDim2.new(0, 4, 0, 2)
local TabSettings = createTab("Settings", SettingsPage)
TabSettings.Position = UDim2.new(0.5, -2, 0, 2)

-- ==============================================
-- พื้นที่สำหรับใส่ฟีเจอร์ทั้งหมด (แท็บ Main)
-- ==============================================
-- ==============================================
-- ESP SYSTEM - เขียนใหม่สำหรับ Hub v12.1
-- ==============================================

-- ตัวแปรหลัก (ใช้จาก GUI หลักที่มีอยู่แล้ว)
local ESP = {
    Enabled = false,
    Objects = {},
    Connections = {},
    LoopConnection = nil,
    LastUpdate = 0,
    Transparency = 0.85
}

-- ฟังก์ชันล้าง ESP
function ESP:Clear()
    for player, data in pairs(self.Objects) do
        if data.Highlight then
            data.Highlight:Destroy()
        end
    end
    self.Objects = {}
    
    for _, conn in pairs(self.Connections) do
        if conn then conn:Disconnect() end
    end
    self.Connections = {}
    
    if self.LoopConnection then
        self.LoopConnection:Disconnect()
        self.LoopConnection = nil
    end
end

-- สร้าง ESP สำหรับผู้เล่น
function ESP:Create(player)
    if player == LocalPlayer then return end
    if not player.Character then return end
    if self.Objects[player] then return end
    
    local char = player.Character
    if not char:FindFirstChild("Humanoid") then return end
    
    local hl = Instance.new("Highlight")
    hl.Name = "XESPHighlight"
    hl.Adornee = char
    hl.DepthMode = Enum.HighlightDepthMode.AlwaysOnTop
    hl.FillColor = player.Team and player.Team.TeamColor.Color or Color3.fromRGB(255, 80, 80)
    hl.FillTransparency = self.Transparency
    hl.OutlineColor = Color3.fromRGB(255, 255, 255)
    hl.OutlineTransparency = 0.2
    
    -- ใส่ใน CoreGui ป้องกันหาย (ใช้ pcall ป้องกัน error)
    pcall(function()
        hl.Parent = game:GetService("CoreGui")
    end)
    if not hl.Parent then
        hl.Parent = workspace.CurrentCamera
    end
    
    self.Objects[player] = {
        Highlight = hl,
        Character = char
    }
end

-- อัพเดท ESP
function ESP:Update()
    if not self.Enabled then return end
    
    -- สร้าง ESP ให้ผู้เล่นที่ยังไม่มี
    for _, player in ipairs(Players:GetPlayers()) do
        if player ~= LocalPlayer and player.Character then
            if not self.Objects[player] or self.Objects[player].Character ~= player.Character then
                self:Create(player)
            end
        end
    end
    
    -- ลบ ESP ของผู้เล่นที่ออกไปแล้ว
    for player, data in pairs(self.Objects) do
        if not player.Parent or not data.Highlight or not data.Highlight.Parent then
            if data.Highlight then data.Highlight:Destroy() end
            self.Objects[player] = nil
        end
    end
end

-- เปิด/ปิด ESP
function ESP:Toggle()
    self.Enabled = not self.Enabled
    
    if self.Enabled then
        self:Clear()
        print("[ESP] เปิดใช้งาน")
        
        -- สร้างให้ผู้เล่นปัจจุบันทันที
        for _, player in ipairs(Players:GetPlayers()) do
            self:Create(player)
        end
        
        -- Loop อัพเดท
        self.LoopConnection = game:GetService("RunService").Heartbeat:Connect(function()
            if tick() - self.LastUpdate >= 0.1 then
                self.LastUpdate = tick()
                self:Update()
            end
        end)
        
        -- Event ผู้เล่นใหม่
        self.Connections.PlayerAdded = Players.PlayerAdded:Connect(function(player)
            task.delay(1, function()
                if self.Enabled then
                    self:Create(player)
                end
            end)
        end)
        
    else
        print("[ESP] ปิดใช้งาน")
        self:Clear()
    end
    
    return self.Enabled
end

-- UI สำหรับ ESP (เข้ากับดีไซน์ GUI คุณ)
local ESPFrame = Instance.new("Frame")
ESPFrame.Size = UDim2.new(1, -30, 0, 110)
ESPFrame.BackgroundColor3 = Color3.fromRGB(35, 35, 48)
ESPFrame.BorderSizePixel = 0
ESPFrame.Parent = MainPage

local ESPStroke = Instance.new("UIStroke")
ESPStroke.Color = Color3.fromRGB(70, 70, 120)
ESPStroke.Thickness = 1
ESPStroke.Parent = ESPFrame

-- หัวข้อ
local ESPTitle = Instance.new("TextLabel")
ESPTitle.Size = UDim2.new(1, -20, 0, 28)
ESPTitle.Position = UDim2.new(0, 10, 0, 8)
ESPTitle.BackgroundTransparency = 1
ESPTitle.Text = "👁 ESP SYSTEM"
ESPTitle.TextColor3 = Color3.fromRGB(220, 220, 255)
ESPTitle.Font = Enum.Font.GothamBold
ESPTitle.TextSize = 15
ESPTitle.TextXAlignment = Enum.TextXAlignment.Left
ESPTitle.Parent = ESPFrame

-- ปุ่ม Toggle
local ESPButton = Instance.new("TextButton")
ESPButton.Size = UDim2.new(0.4, 0, 0, 32)
ESPButton.Position = UDim2.new(0.05, 0, 0, 45)
ESPButton.BackgroundColor3 = Color3.fromRGB(180, 60, 60)
ESPButton.Text = "OFF"
ESPButton.TextColor3 = Color3.new(1, 1, 1)
ESPButton.Font = Enum.Font.GothamBold
ESPButton.TextSize = 14
ESPButton.BorderSizePixel = 0
ESPButton.Parent = ESPFrame

-- ปุ่ม Toggle Trans
local TransButton = Instance.new("TextButton")
TransButton.Size = UDim2.new(0.45, 0, 0, 32)
TransButton.Position = UDim2.new(0.5, 0, 0, 45)
TransButton.BackgroundColor3 = Color3.fromRGB(80, 80, 140)
TransButton.Text = "Transparency: 85%"
TransButton.TextColor3 = Color3.new(1, 1, 1)
TransButton.Font = Enum.Font.Gotham
TransButton.TextSize = 12
TransButton.BorderSizePixel = 0
TransButton.Parent = ESPFrame

-- สถานะ
local ESPStatus = Instance.new("TextLabel")
ESPStatus.Size = UDim2.new(1, -20, 0, 20)
ESPStatus.Position = UDim2.new(0, 10, 0, 82)
ESPStatus.BackgroundTransparency = 1
ESPStatus.Text = "พร้อมใช้งาน"
ESPStatus.TextColor3 = Color3.fromRGB(150, 150, 180)
ESPStatus.Font = Enum.Font.Gotham
ESPStatus.TextSize = 12
ESPStatus.TextXAlignment = Enum.TextXAlignment.Left
ESPStatus.Parent = ESPFrame

-- Hover Effects
ESPButton.MouseEnter:Connect(function()
    local targetColor = ESP.Enabled and Color3.fromRGB(80, 200, 100) or Color3.fromRGB(200, 80, 80)
    TweenService:Create(ESPButton, TweenInfo.new(0.15), {BackgroundColor3 = targetColor}):Play()
end)

ESPButton.MouseLeave:Connect(function()
    local targetColor = ESP.Enabled and Color3.fromRGB(60, 160, 80) or Color3.fromRGB(180, 60, 60)
    TweenService:Create(ESPButton, TweenInfo.new(0.15), {BackgroundColor3 = targetColor}):Play()
end)

TransButton.MouseEnter:Connect(function()
    TweenService:Create(TransButton, TweenInfo.new(0.15), {BackgroundColor3 = Color3.fromRGB(100, 100, 170)}):Play()
end)

TransButton.MouseLeave:Connect(function()
    TweenService:Create(TransButton, TweenInfo.new(0.15), {BackgroundColor3 = Color3.fromRGB(80, 80, 140)}):Play()
end)

-- ฟังก์ชันกดปุ่ม
ESPButton.MouseButton1Click:Connect(function()
    local state = ESP:Toggle()
    
    if state then
        ESPButton.Text = "ON"
        ESPButton.BackgroundColor3 = Color3.fromRGB(60, 160, 80)
        ESPTitle.TextColor3 = Color3.fromRGB(150, 255, 150)
        ESPStatus.Text = "กำลังทำงาน..."
        ESPStatus.TextColor3 = Color3.fromRGB(100, 255, 100)
    else
        ESPButton.Text = "OFF"
        ESPButton.BackgroundColor3 = Color3.fromRGB(180, 60, 60)
        ESPTitle.TextColor3 = Color3.fromRGB(220, 220, 255)
        ESPStatus.Text = "ปิดใช้งาน"
        ESPStatus.TextColor3 = Color3.fromRGB(150, 150, 180)
    end
end)

-- ปรับความโปร่งใส
TransButton.MouseButton1Click:Connect(function()
    if ESP.Transparency == 0.85 then
        ESP.Transparency = 0.6
        TransButton.Text = "Transparency: 60%"
    elseif ESP.Transparency == 0.6 then
        ESP.Transparency = 0.95
        TransButton.Text = "Transparency: 95%"
    else
        ESP.Transparency = 0.85
        TransButton.Text = "Transparency: 85%"
    end
    
    -- อัพเดทที่มีอยู่
    for _, data in pairs(ESP.Objects) do
        if data.Highlight then
            data.Highlight.FillTransparency = ESP.Transparency
        end
    end
end)

-- ล้างตอนปิด GUI
sg.Destroying:Connect(function()
    ESP:Clear()
    print("[ESP] ล้างทรัพยากรเรียบร้อย")
end)

print("[ESP] ระบบ ESP โหลดเสร็จแล้ว")
-- ==============================================
-- [SUPER UPGRADED] Item ESP (Universal Detection)
-- ไม่ต้องระบุชื่อ เจออะไรที่เก็บได้โชว์ทันที
-- ==============================================

local ItemESP = {
    Enabled = false,
    ScanRadius = 500, -- เพิ่มระยะสแกนให้กว้างขึ้น
    ItemsFound = {},
}

-- ฟังก์ชันหัวใจหลัก: ตรวจสอบว่าเป็นไอเท็มหรือไม่จาก "การตอบสนอง" ไม่ใช่ชื่อ
function ItemESP:IsItem(obj)
    -- 1. ต้องเป็น Model หรือ Tool
    if not (obj:IsA("Model") or obj:IsA("Tool") or obj:IsA("BasePart")) then return false end
    
    -- 2. ไม่ใช่ผู้เล่น
    if game.Players:GetPlayerFromCharacter(obj) then return false end
    
    -- 3. หา ProximityPrompt แบบ recursive
    local hasPrompt = false
    pcall(function()
        for _, child in ipairs(obj:GetDescendants()) do
            if child:IsA("ProximityPrompt") then
                hasPrompt = true
                break
            end
        end
    end)
    if hasPrompt then return true end
    
    -- 4. หา ClickDetector
    local hasClick = false
    pcall(function()
        for _, child in ipairs(obj:GetDescendants()) do
            if child:IsA("ClickDetector") then
                hasClick = true
                break
            end
        end
    end)
    if hasClick then return true end
    
    -- 5. มี TouchInterest และอยู่ในโฟลเดอร์ที่เหมาะสม
    if obj:IsA("BasePart") and obj:FindFirstChild("TouchInterest") then
        local parentName = obj.Parent.Name:lower()
        if parentName:find("item") or parentName:find("drop") or parentName:find("spawn") then
            return true
        end
    end
    
    return false
end

function ItemESP:Create(obj)
    if not obj or self.ItemsFound[obj] then return end

    -- หาตำแหน่งที่เหมาะสมสำหรับแสดงผล
    local root = obj:IsA("Model") and (obj.PrimaryPart or obj:FindFirstChildWhichIsA("BasePart")) or obj
    if not root or not root:IsA("BasePart") then return end

    -- 1. ระบบ Highlight (มองทะลุผ่านกำแพง)
    local hl = Instance.new("Highlight")
    hl.Name = "AutoItemESP"
    hl.FillColor = Color3.fromRGB(0, 255, 127) -- สีเขียวมินต์สังเกตง่าย
    hl.FillTransparency = 0.4
    hl.OutlineColor = Color3.new(1, 1, 1)
    hl.OutlineTransparency = 0
    hl.DepthMode = Enum.HighlightDepthMode.AlwaysOnTop
    hl.Parent = obj

    -- 2. ระบบ Billboard (บอกชื่อและระยะทาง)
    local bg = Instance.new("BillboardGui")
    bg.Name = "AutoItemLabel"
    bg.Size = UDim2.new(0, 150, 0, 50)
    bg.AlwaysOnTop = true
    bg.DistanceLowerLimit = 0
    bg.DistanceUpperLimit = self.ScanRadius
    bg.Adornee = root
    
    local txt = Instance.new("TextLabel")
    txt.Size = UDim2.new(1, 0, 1, 0)
    txt.BackgroundTransparency = 1
    txt.TextColor3 = Color3.new(1, 1, 1)
    txt.TextStrokeTransparency = 0
    txt.TextSize = 14
    txt.Font = Enum.Font.SourceSansBold
    txt.Text = obj.Name -- แสดงชื่อดั้งเดิมของ Object
    txt.Parent = bg
    
    bg.Parent = root
    self.ItemsFound[obj] = {Label = txt, Highlight = hl, Gui = bg}

    -- วนลูปอัปเดตระยะทางแบบ Real-time
    task.spawn(function()
        while obj and obj.Parent and self.Enabled do
            if LocalPlayer.Character and LocalPlayer.Character:FindFirstChild("HumanoidRootPart") then
                local dist = (root.Position - LocalPlayer.Character.HumanoidRootPart.Position).Magnitude
                if dist <= self.ScanRadius then
                    txt.Text = obj.Name .. "\n[" .. math.floor(dist) .. "m]"
                    bg.Enabled = true
                    hl.Enabled = true
                else
                    bg.Enabled = false
                    hl.Enabled = false
                end
            end
            task.wait(0.3)
        end
        self:Remove(obj)
    end)
end

function ItemESP:Remove(obj)
    if self.ItemsFound[obj] then
        local data = self.ItemsFound[obj]
        if data.Highlight then data.Highlight:Destroy() end
        if data.Gui then data.Gui:Destroy() end
        self.ItemsFound[obj] = nil
    end
end
function ItemESP:Toggle()
    self.Enabled = not self.Enabled
    
    if self.Enabled then
        print("[ItemESP] เปิดใช้งาน")
        
        -- สแกนครั้งแรก
        self:Update()
        
        -- Loop
        self.LoopConnection = game:GetService("RunService").Heartbeat:Connect(function()
            self:Update()
        end)
        
        -- ติดตามไอเท็มใหม่
        self.Connections.ChildAdded = workspace.ChildAdded:Connect(function(child)
            task.delay(0.3, function()
                if self.Enabled and self:IsItem(child) then
                    self:Create(child)
                end
            end)
        end)
        
    else
        print("[ItemESP] ปิดใช้งาน")
        self:Clear()
    end
    
    return self.Enabled
end

        for obj, _ in pairs(self.ItemsFound) do
            self:Remove(obj)
        end
    else
        -- รันระบบตรวจจับอัตโนมัติ
        task.spawn(function()
            while self.Enabled do
                for _, v in ipairs(workspace:GetDescendants()) do
                    if self:IsItem(v) then
                        self:Create(v)
                    end
                end
                task.wait(3) -- สแกนหาไอเท็มใหม่ทุกๆ 3 วินาที
            end
        end)
    end
end

-- ==============================================
-- UI สำหรับ Item ESP (เข้ากับ GUI หลัก)
-- ==============================================

local ItemFrame = Instance.new("Frame")
ItemFrame.Size = UDim2.new(1, -30, 0, 120)
ItemFrame.BackgroundColor3 = Color3.fromRGB(35, 35, 48)
ItemFrame.BorderSizePixel = 0
ItemFrame.LayoutOrder = 2 -- อยู่ใต้ Player ESP
ItemFrame.Parent = MainPage

local ItemStroke = Instance.new("UIStroke")
ItemStroke.Color = Color3.fromRGB(70, 70, 120)
ItemStroke.Thickness = 1
ItemStroke.Parent = ItemFrame

-- หัวข้อ
local ItemTitle = Instance.new("TextLabel")
ItemTitle.Size = UDim2.new(1, -20, 0, 28)
ItemTitle.Position = UDim2.new(0, 10, 0, 8)
ItemTitle.BackgroundTransparency = 1
ItemTitle.Text = "📦 ITEM ESP (Universal)"
ItemTitle.TextColor3 = Color3.fromRGB(255, 215, 0)
ItemTitle.Font = Enum.Font.GothamBold
ItemTitle.TextSize = 15
ItemTitle.TextXAlignment = Enum.TextXAlignment.Left
ItemTitle.Parent = ItemFrame

-- ปุ่ม Toggle
local ItemToggle = Instance.new("TextButton")
ItemToggle.Size = UDim2.new(0.45, -5, 0, 34)
ItemToggle.Position = UDim2.new(0, 10, 0, 45)
ItemToggle.BackgroundColor3 = Color3.fromRGB(180, 60, 60)
ItemToggle.Text = "OFF"
ItemToggle.TextColor3 = Color3.new(1, 1, 1)
ItemToggle.Font = Enum.Font.GothamBold
ItemToggle.TextSize = 14
ItemToggle.BorderSizePixel = 0
ItemToggle.Parent = ItemFrame

-- ปุ่ม Refresh/Scan
local RefreshBtn = Instance.new("TextButton")
RefreshBtn.Size = UDim2.new(0.5, -5, 0, 34)
RefreshBtn.Position = UDim2.new(0.5, 0, 0, 45)
RefreshBtn.BackgroundColor3 = Color3.fromRGB(80, 80, 140)
RefreshBtn.Text = "Scan Now"
RefreshBtn.TextColor3 = Color3.new(1, 1, 1)
RefreshBtn.Font = Enum.Font.Gotham
RefreshBtn.TextSize = 13
RefreshBtn.BorderSizePixel = 0
RefreshBtn.Parent = ItemFrame

-- สถานะ
local ItemStatus = Instance.new("TextLabel")
ItemStatus.Size = UDim2.new(1, -20, 0, 20)
ItemStatus.Position = UDim2.new(0, 10, 0, 88)
ItemStatus.BackgroundTransparency = 1
ItemStatus.Text = "พร้อมสแกน - รัศมี 200 studs"
ItemStatus.TextColor3 = Color3.fromRGB(150, 150, 180)
ItemStatus.Font = Enum.Font.Gotham
ItemStatus.TextSize = 11
ItemStatus.TextXAlignment = Enum.TextXAlignment.Left
ItemStatus.Parent = ItemFrame

-- Hover Effects
ItemToggle.MouseEnter:Connect(function()
    local color = ItemESP.Enabled and Color3.fromRGB(80, 200, 100) or Color3.fromRGB(200, 80, 80)
    TweenService:Create(ItemToggle, TweenInfo.new(0.15), {BackgroundColor3 = color}):Play()
end)
ItemToggle.MouseLeave:Connect(function()
    local color = ItemESP.Enabled and Color3.fromRGB(60, 160, 80) or Color3.fromRGB(180, 60, 60)
    TweenService:Create(ItemToggle, TweenInfo.new(0.15), {BackgroundColor3 = color}):Play()
end)

RefreshBtn.MouseEnter:Connect(function()
    TweenService:Create(RefreshBtn, TweenInfo.new(0.15), {BackgroundColor3 = Color3.fromRGB(100, 100, 170)}):Play()
end)
RefreshBtn.MouseLeave:Connect(function()
    TweenService:Create(RefreshBtn, TweenInfo.new(0.15), {BackgroundColor3 = Color3.fromRGB(80, 80, 140)}):Play()
end)

-- ฟังก์ชันกดปุ่ม
ItemToggle.MouseButton1Click:Connect(function()
    local state = ItemESP:Toggle()
    
    if state then
        ItemToggle.Text = "ON"
        ItemToggle.BackgroundColor3 = Color3.fromRGB(60, 160, 80)
        ItemTitle.TextColor3 = Color3.fromRGB(150, 255, 150)
        ItemStatus.Text = "กำลังสแกนไอเท็ม..."
        ItemStatus.TextColor3 = Color3.fromRGB(100, 255, 100)
    else
        ItemToggle.Text = "OFF"
        ItemToggle.BackgroundColor3 = Color3.fromRGB(180, 60, 60)
        ItemTitle.TextColor3 = Color3.fromRGB(255, 215, 0)
        ItemStatus.Text = "ปิดใช้งาน"
        ItemStatus.TextColor3 = Color3.fromRGB(150, 150, 180)
    end
end)

RefreshBtn.MouseButton1Click:Connect(function()
    if ItemESP.Enabled then
        ItemESP.LastUpdate = 0 -- บังคับสแกนใหม่ทันที
        ItemStatus.Text = "กำลังรีเฟรช..."
        task.delay(0.5, function()
            if ItemESP.Enabled then
                ItemStatus.Text = "พบ "..tostring(#ItemESP.Objects).." ไอเท็ม"
            end
        end)
    else
        ItemStatus.Text = "กรุณาเปิด ESP ก่อน!"
        ItemStatus.TextColor3 = Color3.fromRGB(255, 100, 100)
        task.wait(1)
        ItemStatus.Text = "พร้อมสแกน - รัศมี 200 studs"
        ItemStatus.TextColor3 = Color3.fromRGB(150, 150, 180)
    end
end)

-- Cleanup
sg.Destroying:Connect(function()
    ItemESP:Clear()
    print("[ItemESP] ล้างทรัพยากรเรียบร้อย")
end)

print("[ItemESP] Item ESP (Universal) โหลดเสร็จแล้ว")
-- ==============================================
-- SPEED CONTROL - ปรับความเร็วลื่นไหล
-- ==============================================

local SpeedSystem = {
    Enabled = false,
    DefaultSpeed = 16,
    TargetSpeed = 16,
    CurrentSpeed = 16,
    Connection = nil
}

function SpeedSystem:SetSpeed(value)
    self.TargetSpeed = math.clamp(value, 1, 200)
    self.Enabled = (value ~= self.DefaultSpeed)
    
    -- เริ่ม Loop ถ้ายังไม่มี
    if not self.Connection then
        self.Connection = game:GetService("RunService").Heartbeat:Connect(function(dt)
            if not self.Enabled then return end
            local char = LocalPlayer.Character
            if not char then return end
            local hum = char:FindFirstChildOfClass("Humanoid")
            if not hum then return end
            
            -- Tween ความเร็วแบบ smooth (Lerp)
            self.CurrentSpeed = self.CurrentSpeed + (self.TargetSpeed - self.CurrentSpeed) * 0.1
            hum.WalkSpeed = self.CurrentSpeed
        end)
    end
end

function SpeedSystem:Reset()
    self:SetSpeed(self.DefaultSpeed)
    if self.Connection then
        self.Connection:Disconnect()
        self.Connection = nil
    end
end

-- UI Speed
local SpeedFrame = Instance.new("Frame")
SpeedFrame.Size = UDim2.new(1, -30, 0, 100)
SpeedFrame.BackgroundColor3 = Color3.fromRGB(35, 35, 48)
SpeedFrame.BorderSizePixel = 0
SpeedFrame.LayoutOrder = 3
SpeedFrame.Parent = MainPage

local SpeedStroke = Instance.new("UIStroke", SpeedFrame)
SpeedStroke.Color = Color3.fromRGB(70, 70, 120)
SpeedStroke.Thickness = 1

local SpeedTitle = Instance.new("TextLabel")
SpeedTitle.Size = UDim2.new(1, -20, 0, 24)
SpeedTitle.Position = UDim2.new(0, 10, 0, 8)
SpeedTitle.BackgroundTransparency = 1
SpeedTitle.Text = "⚡ SPEED BOOST"
SpeedTitle.TextColor3 = Color3.fromRGB(100, 200, 255)
SpeedTitle.Font = Enum.Font.GothamBold
SpeedTitle.TextSize = 15
SpeedTitle.TextXAlignment = Enum.TextXAlignment.Left
SpeedTitle.Parent = SpeedFrame

-- แถบเลื่อนความเร็ว
local SpeedSlider = Instance.new("TextButton")
SpeedSlider.Size = UDim2.new(1, -30, 0, 30)
SpeedSlider.Position = UDim2.new(0, 15, 0, 40)
SpeedSlider.BackgroundColor3 = Color3.fromRGB(60, 60, 90)
SpeedSlider.Text = "Speed: 16 (Default)"
SpeedSlider.TextColor3 = Color3.new(1, 1, 1)
SpeedSlider.Font = Enum.Font.Gotham
SpeedSlider.TextSize = 13
SpeedSlider.BorderSizePixel = 0
SpeedSlider.Parent = SpeedFrame

-- ปุ่มลด/เพิ่ม
local function createSpeedBtn(text, posX, change)
    local btn = Instance.new("TextButton")
    btn.Size = UDim2.new(0, 50, 0, 30)
    btn.Position = UDim2.new(posX, 0, 0, 40)
    btn.BackgroundColor3 = Color3.fromRGB(80, 80, 140)
    btn.Text = text
    btn.TextColor3 = Color3.new(1, 1, 1)
    btn.Font = Enum.Font.GothamBold
    btn.TextSize = 14
    btn.BorderSizePixel = 0
    btn.Parent = SpeedFrame
    
    btn.MouseButton1Click:Connect(function()
        local newSpeed = SpeedSystem.TargetSpeed + change
        SpeedSystem:SetSpeed(newSpeed)
        SpeedSlider.Text = "Speed: " .. math.floor(newSpeed)
        SpeedTitle.TextColor3 = (newSpeed > 16) and Color3.fromRGB(150, 255, 150) or Color3.fromRGB(100, 200, 255)
    end)
    
    btn.MouseEnter:Connect(function()
        TweenService:Create(btn, TweenInfo.new(0.15), {BackgroundColor3 = Color3.fromRGB(100, 100, 170)}):Play()
    end)
    btn.MouseLeave:Connect(function()
        TweenService:Create(btn, TweenInfo.new(0.15), {BackgroundColor3 = Color3.fromRGB(80, 80, 140)}):Play()
    end)
end

createSpeedBtn("-", 0.05, -5)
createSpeedBtn("+", 0.82, 5)

-- ปุ่มรีเซ็ต
local SpeedReset = Instance.new("TextButton")
SpeedReset.Size = UDim2.new(0.9, 0, 0, 25)
SpeedReset.Position = UDim2.new(0.05, 0, 0, 75)
SpeedReset.BackgroundColor3 = Color3.fromRGB(140, 60, 60)
SpeedReset.Text = "Reset Speed"
SpeedReset.TextColor3 = Color3.new(1, 1, 1)
SpeedReset.Font = Enum.Font.Gotham
SpeedReset.TextSize = 12
SpeedReset.BorderSizePixel = 0
SpeedReset.Parent = SpeedFrame

SpeedReset.MouseButton1Click:Connect(function()
    SpeedSystem:Reset()
    SpeedSlider.Text = "Speed: 16 (Default)"
    SpeedTitle.TextColor3 = Color3.fromRGB(100, 200, 255)
end)

sg.Destroying:Connect(function()
    SpeedSystem:Reset()
end)

print("[Speed] Speed Control Loaded")
-- ==============================================
-- JUMP POWER - ปรับแรงกระโดดสูง
-- ==============================================

local JumpSystem = {
    Enabled = false,
    DefaultPower = 50,
    TargetPower = 50,
    Connection = nil
}

function JumpSystem:SetPower(value)
    self.TargetPower = math.clamp(value, 1, 300)
    self.Enabled = (value ~= self.DefaultPower)
    
    if not self.Connection then
        self.Connection = game:GetService("RunService").Heartbeat:Connect(function()
            if not self.Enabled then return end
            local char = LocalPlayer.Character
            if not char then return end
            local hum = char:FindFirstChildOfClass("Humanoid")
            if not hum then return end
            
            -- เปลี่ยน JumpPower ทันที (ไม่ต้อง tween เพราะกระโดดเป็นเหตุการณ์ทันที)
            if hum.JumpPower ~= self.TargetPower then
                hum.JumpPower = self.TargetPower
            end
        end)
    end
end

function JumpSystem:Reset()
    self:SetPower(self.DefaultPower)
    if self.Connection then
        self.Connection:Disconnect()
        self.Connection = nil
    end
end

-- UI Jump
local JumpFrame = Instance.new("Frame")
JumpFrame.Size = UDim2.new(1, -30, 0, 100)
JumpFrame.BackgroundColor3 = Color3.fromRGB(35, 35, 48)
JumpFrame.BorderSizePixel = 0
JumpFrame.LayoutOrder = 4
JumpFrame.Parent = MainPage

local JumpStroke = Instance.new("UIStroke", JumpFrame)
JumpStroke.Color = Color3.fromRGB(70, 70, 120)
JumpStroke.Thickness = 1

local JumpTitle = Instance.new("TextLabel")
JumpTitle.Size = UDim2.new(1, -20, 0, 24)
JumpTitle.Position = UDim2.new(0, 10, 0, 8)
JumpTitle.BackgroundTransparency = 1
JumpTitle.Text = "🦘 JUMP POWER"
JumpTitle.TextColor3 = Color3.fromRGB(255, 150, 100)
JumpTitle.Font = Enum.Font.GothamBold
JumpTitle.TextSize = 15
JumpTitle.TextXAlignment = Enum.TextXAlignment.Left
JumpTitle.Parent = JumpFrame

-- ปุ่มแรงกระโดด
local JumpSlider = Instance.new("TextLabel")
JumpSlider.Size = UDim2.new(1, -120, 0, 30)
JumpSlider.Position = UDim2.new(0, 60, 0, 40)
JumpSlider.BackgroundColor3 = Color3.fromRGB(60, 60, 90)
JumpSlider.Text = "Power: 50"
JumpSlider.TextColor3 = Color3.new(1, 1, 1)
JumpSlider.Font = Enum.Font.Gotham
JumpSlider.TextSize = 13
JumpSlider.BorderSizePixel = 0
JumpSlider.Parent = JumpFrame

local function createJumpBtn(text, offset, power)
    local btn = Instance.new("TextButton")
    btn.Size = UDim2.new(0, 50, 0, 30)
    btn.Position = UDim2.new(0.05, 0, 0, 40)
    btn.Position = offset and UDim2.new(0.82, 0, 0, 40) or UDim2.new(0.05, 0, 0, 40)
    btn.BackgroundColor3 = Color3.fromRGB(80, 80, 140)
    btn.Text = text
    btn.TextColor3 = Color3.new(1, 1, 1)
    btn.Font = Enum.Font.GothamBold
    btn.TextSize = 14
    btn.BorderSizePixel = 0
    btn.Parent = JumpFrame
    
    btn.MouseButton1Click:Connect(function()
        JumpSystem:SetPower(power)
        JumpSlider.Text = "Power: " .. power
        JumpTitle.TextColor3 = (power > 50) and Color3.fromRGB(150, 255, 150) or Color3.fromRGB(255, 150, 100)
    end)
    
    btn.MouseEnter:Connect(function()
        TweenService:Create(btn, TweenInfo.new(0.15), {BackgroundColor3 = Color3.fromRGB(100, 100, 170)}):Play()
    end)
    btn.MouseLeave:Connect(function()
        TweenService:Create(btn, TweenInfo.new(0.15), {BackgroundColor3 = Color3.fromRGB(80, 80, 140)}):Play()
    end)
end

createJumpBtn("-", false, math.max(10, JumpSystem.TargetPower - 10))
createJumpBtn("+", true, math.min(300, JumpSystem.TargetPower + 10))

-- ปุ่ม Super Jump (กระโดดสูงมาก)
local SuperJump = Instance.new("TextButton")
SuperJump.Size = UDim2.new(0.45, -5, 0, 25)
SuperJump.Position = UDim2.new(0.05, 0, 0, 75)
SuperJump.BackgroundColor3 = Color3.fromRGB(200, 150, 50)
SuperJump.Text = "Super Jump"
SuperJump.TextColor3 = Color3.new(0, 0, 0)
SuperJump.Font = Enum.Font.GothamBold
SuperJump.TextSize = 12
SuperJump.BorderSizePixel = 0
SuperJump.Parent = JumpFrame

SuperJump.MouseButton1Click:Connect(function()
    JumpSystem:SetPower(150)
    JumpSlider.Text = "Power: 150 (Super)"
    JumpTitle.TextColor3 = Color3.fromRGB(255, 255, 100)
end)

SuperJump.MouseEnter:Connect(function()
    TweenService:Create(SuperJump, TweenInfo.new(0.15), {BackgroundColor3 = Color3.fromRGB(255, 200, 80)}):Play()
end)
SuperJump.MouseLeave:Connect(function()
    TweenService:Create(SuperJump, TweenInfo.new(0.15), {BackgroundColor3 = Color3.fromRGB(200, 150, 50)}):Play()
end)

-- ปุ่มรีเซ็ต
local JumpReset = Instance.new("TextButton")
JumpReset.Size = UDim2.new(0.45, -5, 0, 25)
JumpReset.Position = UDim2.new(0.5, 5, 0, 75)
JumpReset.BackgroundColor3 = Color3.fromRGB(140, 60, 60)
JumpReset.Text = "Reset"
JumpReset.TextColor3 = Color3.new(1, 1, 1)
JumpReset.Font = Enum.Font.Gotham
JumpReset.TextSize = 12
JumpReset.BorderSizePixel = 0
JumpReset.Parent = JumpFrame

JumpReset.MouseButton1Click:Connect(function()
    JumpSystem:Reset()
    JumpSlider.Text = "Power: 50 (Default)"
    JumpTitle.TextColor3 = Color3.fromRGB(255, 150, 100)
end)

sg.Destroying:Connect(function()
    JumpSystem:Reset()
end)

print("[Jump] Jump Control Loaded")
-- ==============================================
-- XRAY VISION - มองผ่านผนัง (สั้น ใช้ได้จริง)
-- ==============================================

local Xray = {
    Enabled = false,
    Objects = {}, -- [Part] = OriginalTransparency
    Connection = nil,
    Range = 100
}

function Xray:Scan()
    if not self.Enabled then return end
    if not LocalPlayer.Character then return end
    local hrp = LocalPlayer.Character:FindFirstChild("HumanoidRootPart")
    if not hrp then return end
    
    local pos = hrp.Position
    local processed = {}
    
    -- หา Parts ในรัศมี (ใช้ FindPartsInBall เร็วกว่า)
    local parts = workspace:GetPartBoundsInRadius(pos, self.Range)
    
    for _, part in ipairs(parts) do
        if part.Anchored and part.Transparency < 0.9 then
            -- ข้ามตัวละคร, Terrain, น้ำ
            if part:IsDescendantOf(LocalPlayer.Character) then continue end
            if part.Parent and part.Parent:FindFirstChild("Humanoid") then continue end
            if part:IsA("Terrain") or part.Name:lower():find("terrain") then continue end
            
            processed[part] = true
            
            -- บันทึกค่าเดิมถ้ายังไม่มี
            if not self.Objects[part] then
                self.Objects[part] = part.Transparency
            end
            
            -- ทำโปร่งใส
            part.Transparency = 0.75
        end
    end
    
    -- คืนค่าส่วนที่ออกจากระยะ
    for part, original in pairs(self.Objects) do
        if not processed[part] then
            if part and part.Parent then
                part.Transparency = original
            end
            self.Objects[part] = nil
        end
    end
end

function Xray:Toggle()
    self.Enabled = not self.Enabled
    
    if self.Enabled then
        print("[Xray] เปิดใช้งาน")
        self.Connection = game:GetService("RunService").Heartbeat:Connect(function()
            if tick() % 0.5 < 0.03 then -- สแกนทุก 0.5 วินาที
                self:Scan()
            end
        end)
    else
        print("[Xray] ปิดใช้งาน")
        if self.Connection then self.Connection:Disconnect() end
        -- คืนค่าเดิมทั้งหมด
        for part, original in pairs(self.Objects) do
            if part and part.Parent then
                part.Transparency = original
            end
        end
        self.Objects = {}
    end
    
    return self.Enabled
end

-- UI Xray (อยู่ใต้ Jump)
local XrayFrame = Instance.new("Frame")
XrayFrame.Size = UDim2.new(1, -30, 0, 90)
XrayFrame.BackgroundColor3 = Color3.fromRGB(35, 35, 48)
XrayFrame.BorderSizePixel = 0
XrayFrame.LayoutOrder = 5 -- ต่อจาก Jump
XrayFrame.Parent = MainPage

local XrayStroke = Instance.new("UIStroke", XrayFrame)
XrayStroke.Color = Color3.fromRGB(70, 70, 120)
XrayStroke.Thickness = 1

local XrayTitle = Instance.new("TextLabel")
XrayTitle.Size = UDim2.new(1, -20, 0, 26)
XrayTitle.Position = UDim2.new(0, 10, 0, 8)
XrayTitle.BackgroundTransparency = 1
XrayTitle.Text = "👁 XRAY VISION"
XrayTitle.TextColor3 = Color3.fromRGB(100, 200, 255)
XrayTitle.Font = Enum.Font.GothamBold
XrayTitle.TextSize = 15
XrayTitle.TextXAlignment = Enum.TextXAlignment.Left
XrayTitle.Parent = XrayFrame

local XrayBtn = Instance.new("TextButton")
XrayBtn.Size = UDim2.new(0.45, -5, 0, 36)
XrayBtn.Position = UDim2.new(0, 10, 0, 42)
XrayBtn.BackgroundColor3 = Color3.fromRGB(180, 60, 60)
XrayBtn.Text = "OFF"
XrayBtn.TextColor3 = Color3.new(1, 1, 1)
XrayBtn.Font = Enum.Font.GothamBold
XrayBtn.TextSize = 14
XrayBtn.BorderSizePixel = 0
XrayBtn.Parent = XrayFrame

local XrayStatus = Instance.new("TextLabel")
XrayStatus.Size = UDim2.new(0.5, 0, 0, 36)
XrayStatus.Position = UDim2.new(0.5, 5, 0, 42)
XrayStatus.BackgroundTransparency = 1
XrayStatus.Text = "พร้อมใช้งาน"
XrayStatus.TextColor3 = Color3.fromRGB(150, 150, 180)
XrayStatus.Font = Enum.Font.Gotham
XrayStatus.TextSize = 12
XrayStatus.TextXAlignment = Enum.TextXAlignment.Left
XrayStatus.Parent = XrayFrame

-- Hover Effects
XrayBtn.MouseEnter:Connect(function()
    local color = Xray.Enabled and Color3.fromRGB(80, 200, 100) or Color3.fromRGB(200, 80, 80)
    TweenService:Create(XrayBtn, TweenInfo.new(0.15), {BackgroundColor3 = color}):Play()
end)

XrayBtn.MouseLeave:Connect(function()
    local color = Xray.Enabled and Color3.fromRGB(60, 160, 80) or Color3.fromRGB(180, 60, 60)
    TweenService:Create(XrayBtn, TweenInfo.new(0.15), {BackgroundColor3 = color}):Play()
end)

-- ฟังก์ชันกดปุ่ม
XrayBtn.MouseButton1Click:Connect(function()
    local state = Xray:Toggle()
    
    if state then
        XrayBtn.Text = "ON"
        XrayBtn.BackgroundColor3 = Color3.fromRGB(60, 160, 80)
        XrayTitle.TextColor3 = Color3.fromRGB(150, 255, 150)
        XrayStatus.Text = "กำลังมองผ่านผนัง..."
        XrayStatus.TextColor3 = Color3.fromRGB(100, 255, 100)
    else
        XrayBtn.Text = "OFF"
        XrayBtn.BackgroundColor3 = Color3.fromRGB(180, 60, 60)
        XrayTitle.TextColor3 = Color3.fromRGB(100, 200, 255)
        XrayStatus.Text = "ปิดใช้งาน"
        XrayStatus.TextColor3 = Color3.fromRGB(150, 150, 180)
    end
end)

-- Cleanup เมื่อปิด GUI
sg.Destroying:Connect(function()
    Xray.Enabled = false
    if Xray.Connection then Xray.Connection:Disconnect() end
    for part, original in pairs(Xray.Objects) do
        if part and part.Parent then
            part.Transparency = original
        end
    end
end)

print("[Xray] Xray System Loaded")
-- ==============================================
-- NOCLIP - เดินทะลุผนัง (สั้น ไม่มีบิน ไม่ยุ่งยาก)
-- ==============================================

local Noclip = {
    Enabled = false,
    Connection = nil
}

function Noclip:Toggle()
    self.Enabled = not self.Enabled
    
    if self.Enabled then
        print("[Noclip] เปิดใช้งาน")
        
        self.Connection = game:GetService("RunService").Stepped:Connect(function()
            if not self.Enabled then return end
            local char = LocalPlayer.Character
            if not char then return end
            
            -- ปิดการชนทุกส่วนในตัวละคร
            for _, part in ipairs(char:GetDescendants()) do
                if part:IsA("BasePart") then
                    part.CanCollide = false
                end
            end
        end)
    else
        print("[Noclip] ปิดใช้งาน")
        if self.Connection then
            self.Connection:Disconnect()
            self.Connection = nil
        end
        
        -- คืนค่าการชน (เปิดใหม่)
        local char = LocalPlayer.Character
        if char then
            for _, part in ipairs(char:GetDescendants()) do
                if part:IsA("BasePart") then
                    part.CanCollide = true
                end
            end
        end
    end
    
    return self.Enabled
end

-- UI Noclip (อยู่ใต้ Xray)
local NoclipFrame = Instance.new("Frame")
NoclipFrame.Size = UDim2.new(1, -30, 0, 80)
NoclipFrame.BackgroundColor3 = Color3.fromRGB(35, 35, 48)
NoclipFrame.BorderSizePixel = 0
NoclipFrame.LayoutOrder = 6 -- ต่อจาก Xray
NoclipFrame.Parent = MainPage

local NoclipStroke = Instance.new("UIStroke", NoclipFrame)
NoclipStroke.Color = Color3.fromRGB(70, 70, 120)
NoclipStroke.Thickness = 1

local NoclipTitle = Instance.new("TextLabel")
NoclipTitle.Size = UDim2.new(1, -20, 0, 26)
NoclipTitle.Position = UDim2.new(0, 10, 0, 8)
NoclipTitle.BackgroundTransparency = 1
NoclipTitle.Text = "👻 NOCLIP"
NoclipTitle.TextColor3 = Color3.fromRGB(255, 150, 50)
NoclipTitle.Font = Enum.Font.GothamBold
NoclipTitle.TextSize = 15
NoclipTitle.TextXAlignment = Enum.TextXAlignment.Left
NoclipTitle.Parent = NoclipFrame

local NoclipBtn = Instance.new("TextButton")
NoclipBtn.Size = UDim2.new(0.9, 0, 0, 36)
NoclipBtn.Position = UDim2.new(0.05, 0, 0, 38)
NoclipBtn.BackgroundColor3 = Color3.fromRGB(180, 60, 60)
NoclipBtn.Text = "OFF (เดินทะลุผนัง)"
NoclipBtn.TextColor3 = Color3.new(1, 1, 1)
NoclipBtn.Font = Enum.Font.GothamBold
NoclipBtn.TextSize = 14
NoclipBtn.BorderSizePixel = 0
NoclipBtn.Parent = NoclipFrame

-- Hover Effects
NoclipBtn.MouseEnter:Connect(function()
    local color = Noclip.Enabled and Color3.fromRGB(80, 200, 100) or Color3.fromRGB(200, 80, 80)
    TweenService:Create(NoclipBtn, TweenInfo.new(0.15), {BackgroundColor3 = color}):Play()
end)

NoclipBtn.MouseLeave:Connect(function()
    local color = Noclip.Enabled and Color3.fromRGB(60, 160, 80) or Color3.fromRGB(180, 60, 60)
    TweenService:Create(NoclipBtn, TweenInfo.new(0.15), {BackgroundColor3 = color}):Play()
end)

-- กดปุ่ม
NoclipBtn.MouseButton1Click:Connect(function()
    local state = Noclip:Toggle()
    
    if state then
        NoclipBtn.Text = "ON (เดินทะลุได้)"
        NoclipBtn.BackgroundColor3 = Color3.fromRGB(60, 160, 80)
        NoclipTitle.TextColor3 = Color3.fromRGB(150, 255, 150)
    else
        NoclipBtn.Text = "OFF (เดินทะลุผนัง)"
        NoclipBtn.BackgroundColor3 = Color3.fromRGB(180, 60, 60)
        NoclipTitle.TextColor3 = Color3.fromRGB(255, 150, 50)
    end
end)

-- Cleanup เมื่อปิด GUI
sg.Destroying:Connect(function()
    Noclip.Enabled = false
    if Noclip.Connection then Noclip.Connection:Disconnect() end
    -- คืนค่าการชน
    local char = LocalPlayer.Character
    if char then
        for _, part in ipairs(char:GetDescendants()) do
            if part:IsA("BasePart") then
                part.CanCollide = true
            end
        end
    end
end)

print("[Noclip] Noclip Loaded (Mobile Supported)")
-- ==============================================
-- AIMBOT v2.0 - เล็งตาม FOV หรือ ระยะทาง (ใกล้สุด)
-- ==============================================

local Aimbot = {
    Enabled = false,
    Target = nil,
    Connection = nil,
    Mode = "FOV", -- "FOV" หรือ "Distance"
    Settings = {
        FOV = 150,           -- ใช้ตอนโหมด FOV
        MaxDistance = 200,   -- ใช้ตอนโหมด Distance ( studs )
        Smoothness = 0.15,
        TargetPart = "Head",
        TeamCheck = true
    }
}

function Aimbot:GetClosest()
    if not LocalPlayer.Character then return nil end
    local root = LocalPlayer.Character:FindFirstChild("HumanoidRootPart")
    if not root then return nil end
    
    local camera = workspace.CurrentCamera
    local center = Vector2.new(camera.ViewportSize.X/2, camera.ViewportSize.Y/2)
    local closest = nil
    
    -- เลือก metric ตามโหมด
    local bestMetric = math.huge
    
    for _, player in ipairs(Players:GetPlayers()) do
        if player ~= LocalPlayer and player.Character then
            local char = player.Character
            local humanoid = char:FindFirstChildOfClass("Humanoid")
            
            -- Team Check
            if self.Settings.TeamCheck and LocalPlayer.Team and player.Team then
                if LocalPlayer.Team == player.Team then continue end
            end
            
            if humanoid and humanoid.Health > 0 then
                local targetPart = char:FindFirstChild(self.Settings.TargetPart) 
                                  or char:FindFirstChild("HumanoidRootPart")
                                  or char:FindFirstChild("Head")
                
                if targetPart then
                    local screenPos, onScreen = camera:WorldToViewportPoint(targetPart.Position)
                    
                    -- ต้องอยู่บนหน้าจอเท่านั้น (ไม่สนใจคนข้างหลัง)
                    if onScreen then
                        local metric = math.huge
                        local valid = false
                        
                        if self.Mode == "FOV" then
                            -- โหมดเดิม: ใกล้กลางจอ = ดี
                            local screenDist = (Vector2.new(screenPos.X, screenPos.Y) - center).Magnitude
                            if screenDist <= self.Settings.FOV then
                                metric = screenDist
                                valid = true
                            end
                        else
                            -- โหมดใหม่: ใกล้ตัวละคร = ดี
                            local dist3D = (root.Position - targetPart.Position).Magnitude
                            if dist3D <= self.Settings.MaxDistance then
                                metric = dist3D
                                valid = true
                            end
                        end
                        
                        if valid and metric < bestMetric then
                            bestMetric = metric
                            closest = targetPart
                        end
                    end
                end
            end
        end
    end
    
    return closest
end

function Aimbot:Update()
    if not self.Enabled then return end
    
    local target = self:GetClosest()
    if target and target.Parent then
        self.Target = target
        local camera = workspace.CurrentCamera
        
        -- คำนวณ CFrame ใหม่
        local targetCF = CFrame.new(camera.CFrame.Position, target.Position)
        
        -- Smooth Lerp
        camera.CFrame = camera.CFrame:Lerp(targetCF, self.Settings.Smoothness)
    else
        self.Target = nil
    end
end

function Aimbot:Toggle()
    self.Enabled = not self.Enabled
    
    if self.Enabled then
        print("[Aimbot] เปิดใช้งาน | โหมด:", self.Mode)
        self.Connection = game:GetService("RunService").RenderStepped:Connect(function()
            self:Update()
        end)
    else
        print("[Aimbot] ปิดใช้งาน")
        if self.Connection then
            self.Connection:Disconnect()
            self.Connection = nil
        end
    end
    
    return self.Enabled
end

-- ==============================================
-- UI สำหรับ Aimbot v2.0 (มีปุ่มสลับโหมด)
-- ==============================================

local AimFrame = Instance.new("Frame")
AimFrame.Size = UDim2.new(1, -30, 0, 140) -- เพิ่มความสูงเป็น 140
AimFrame.BackgroundColor3 = Color3.fromRGB(35, 35, 48)
AimFrame.BorderSizePixel = 0
AimFrame.LayoutOrder = 7
AimFrame.Parent = MainPage

local AimStroke = Instance.new("UIStroke", AimFrame)
AimStroke.Color = Color3.fromRGB(70, 70, 120)
AimStroke.Thickness = 1

-- หัวข้อ
local AimTitle = Instance.new("TextLabel")
AimTitle.Size = UDim2.new(1, -20, 0, 26)
AimTitle.Position = UDim2.new(0, 10, 0, 8)
AimTitle.BackgroundTransparency = 1
AimTitle.Text = "🎯 AIMBOT v2.0"
AimTitle.TextColor3 = Color3.fromRGB(255, 100, 100)
AimTitle.Font = Enum.Font.GothamBold
AimTitle.TextSize = 15
AimTitle.TextXAlignment = Enum.TextXAlignment.Left
AimTitle.Parent = AimFrame

-- สถานะ (แสดงโหมดปัจจุบัน)
local AimStatus = Instance.new("TextLabel")
AimStatus.Size = UDim2.new(1, -20, 0, 20)
AimStatus.Position = UDim2.new(0, 10, 0, 32)
AimStatus.BackgroundTransparency = 1
AimStatus.Text = "Mode: FOV (Closest to crosshair)"
AimStatus.TextColor3 = Color3.fromRGB(180, 180, 220)
AimStatus.Font = Enum.Font.Gotham
AimStatus.TextSize = 11
AimStatus.TextXAlignment = Enum.TextXAlignment.Left
AimStatus.Parent = AimFrame

-- ปุ่ม Toggle หลัก (ซ้าย)
local AimBtn = Instance.new("TextButton")
AimBtn.Size = UDim2.new(0.45, -5, 0, 45)
AimBtn.Position = UDim2.new(0, 10, 0, 58)
AimBtn.BackgroundColor3 = Color3.fromRGB(180, 60, 60)
AimBtn.Text = "OFF"
AimBtn.TextColor3 = Color3.new(1, 1, 1)
AimBtn.Font = Enum.Font.GothamBold
AimBtn.TextSize = 14
AimBtn.BorderSizePixel = 0
AimBtn.Parent = AimFrame

-- ปุ่มสลับโหมด (ขวาบน)
local ModeBtn = Instance.new("TextButton")
ModeBtn.Size = UDim2.new(0.48, -5, 0, 20)
ModeBtn.Position = UDim2.new(0.52, 0, 0, 58)
ModeBtn.BackgroundColor3 = Color3.fromRGB(80, 140, 180) -- สีฟ้า = FOV Mode
ModeBtn.Text = "Mode: FOV"
ModeBtn.TextColor3 = Color3.new(1, 1, 1)
ModeBtn.Font = Enum.Font.GothamBold
ModeBtn.TextSize = 11
ModeBtn.BorderSizePixel = 0
ModeBtn.Parent = AimFrame

-- ปุ่มเปลี่ยนเป้า Head/Body (ขวาล่าง)
local PartBtn = Instance.new("TextButton")
PartBtn.Size = UDim2.new(0.48, -5, 0, 22)
PartBtn.Position = UDim2.new(0.52, 0, 0, 81)
PartBtn.BackgroundColor3 = Color3.fromRGB(80, 80, 140)
PartBtn.Text = "Target: Head"
PartBtn.TextColor3 = Color3.new(1, 1, 1)
PartBtn.Font = Enum.Font.Gotham
PartBtn.TextSize = 11
PartBtn.BorderSizePixel = 0
PartBtn.Parent = AimFrame

-- Hover Effects
AimBtn.MouseEnter:Connect(function()
    local color = Aimbot.Enabled and Color3.fromRGB(80, 200, 100) or Color3.fromRGB(200, 80, 80)
    TweenService:Create(AimBtn, TweenInfo.new(0.15), {BackgroundColor3 = color}):Play()
end)
AimBtn.MouseLeave:Connect(function()
    local color = Aimbot.Enabled and Color3.fromRGB(60, 160, 80) or Color3.fromRGB(180, 60, 60)
    TweenService:Create(AimBtn, TweenInfo.new(0.15), {BackgroundColor3 = color}):Play()
end)

ModeBtn.MouseEnter:Connect(function()
    TweenService:Create(ModeBtn, TweenInfo.new(0.15), {BackgroundColor3 = Color3.fromRGB(100, 170, 200)}):Play()
end)
ModeBtn.MouseLeave:Connect(function()
    local isFOV = Aimbot.Mode == "FOV"
    local color = isFOV and Color3.fromRGB(80, 140, 180) or Color3.fromRGB(180, 140, 80)
    TweenService:Create(ModeBtn, TweenInfo.new(0.15), {BackgroundColor3 = color}):Play()
end)

PartBtn.MouseEnter:Connect(function()
    TweenService:Create(PartBtn, TweenInfo.new(0.15), {BackgroundColor3 = Color3.fromRGB(100, 100, 170)}):Play()
end)
PartBtn.MouseLeave:Connect(function()
    local isHead = Aimbot.Settings.TargetPart == "Head"
    local color = isHead and Color3.fromRGB(80, 80, 140) or Color3.fromRGB(140, 100, 80)
    TweenService:Create(PartBtn, TweenInfo.new(0.15), {BackgroundColor3 = color}):Play()
end)

-- กดปุ่ม Toggle
AimBtn.MouseButton1Click:Connect(function()
    local state = Aimbot:Toggle()
    
    if state then
        AimBtn.Text = "ON"
        AimBtn.BackgroundColor3 = Color3.fromRGB(60, 160, 80)
        AimTitle.TextColor3 = Color3.fromRGB(150, 255, 150)
    else
        AimBtn.Text = "OFF"
        AimBtn.BackgroundColor3 = Color3.fromRGB(180, 60, 60)
        AimTitle.TextColor3 = Color3.fromRGB(255, 100, 100)
    end
end)

-- 🆕 สลับโหมด FOV / Distance
ModeBtn.MouseButton1Click:Connect(function()
    if Aimbot.Mode == "FOV" then
        -- เปลี่ยนเป็น Distance
        Aimbot.Mode = "Distance"
        ModeBtn.Text = "Mode: Distance"
        ModeBtn.BackgroundColor3 = Color3.fromRGB(180, 140, 80) -- ส้ม
        AimStatus.Text = "Mode: Closest by Distance (200 studs)"
        AimStatus.TextColor3 = Color3.fromRGB(255, 200, 100)
        print("[Aimbot] เปลี่ยนโหมด: Distance (ล็อคคนใกล้สุด)")
    else
        -- เปลี่ยนเป็น FOV
        Aimbot.Mode = "FOV"
        ModeBtn.Text = "Mode: FOV"
        ModeBtn.BackgroundColor3 = Color3.fromRGB(80, 140, 180) -- ฟ้า
        AimStatus.Text = "Mode: FOV (Closest to crosshair)"
        AimStatus.TextColor3 = Color3.fromRGB(180, 180, 220)
        print("[Aimbot] เปลี่ยนโหมด: FOV (ล็อคคนใกล้กลางจอ)")
    end
end)

-- เปลี่ยนเป้า Head/Body
PartBtn.MouseButton1Click:Connect(function()
    if Aimbot.Settings.TargetPart == "Head" then
        Aimbot.Settings.TargetPart = "HumanoidRootPart"
        PartBtn.Text = "Target: Body"
        PartBtn.BackgroundColor3 = Color3.fromRGB(140, 100, 80)
    else
        Aimbot.Settings.TargetPart = "Head"
        PartBtn.Text = "Target: Head"
        PartBtn.BackgroundColor3 = Color3.fromRGB(80, 80, 140)
    end
end)

-- Cleanup
sg.Destroying:Connect(function()
    Aimbot.Enabled = false
    if Aimbot.Connection then Aimbot.Connection:Disconnect() end
end)

print("[Aimbot] Aimbot v2.0 Loaded")
print("[Aimbot] Modes: FOV (screen center) | Distance (3D proximity)")
-- ==============================================
-- TELEPORT TO PLAYER - เทเลพอร์ตไปหาผู้เล่น
-- ==============================================

local Teleport = {
    Enabled = false,
    Window = nil,
    ListButtons = {},
    Connection = nil
}

-- ฟังก์ชันเทเลพอร์ต (มีป้องกันติดพื้น)
function Teleport:To(player)
    if not player or player == LocalPlayer then return end
    if not player.Character then 
        print("[Teleport] ผู้เล่นไม่มีตัวละคร")
        return 
    end
    
    local targetHRP = player.Character:FindFirstChild("HumanoidRootPart")
    local myChar = LocalPlayer.Character
    local myHRP = myChar and myChar:FindFirstChild("HumanoidRootPart")
    
    if not targetHRP or not myHRP then return end
    
    -- คำนวณตำแหน่ง: ด้านหลัง 5 studs + สูงขึ้น 3 studs (ป้องกันติดพื้น)
    local destination = targetHRP.CFrame * CFrame.new(0, 0, 6) + Vector3.new(0, 3, 0)
    
    pcall(function()
        myHRP.CFrame = destination
        -- ป้องกันกระเด็นเพราะความเร็ว
        myHRP.Velocity = Vector3.new(0, 0, 0)
    end)
    
    print("[Teleport] เทเลพอร์ตไปหา: " .. player.Name)
end

-- ฟังก์ชันเทเลพอร์ตไปผู้เล่นที่ใกล้ที่สุด
function Teleport:ToClosest()
    local myHRP = LocalPlayer.Character and LocalPlayer.Character:FindFirstChild("HumanoidRootPart")
    if not myHRP then return end
    
    local closest = nil
    local minDist = math.huge
    
    for _, player in ipairs(Players:GetPlayers()) do
        if player ~= LocalPlayer and player.Character then
            local hrp = player.Character:FindFirstChild("HumanoidRootPart")
            if hrp then
                local dist = (myHRP.Position - hrp.Position).Magnitude
                if dist < minDist then
                    minDist = dist
                    closest = player
                end
            end
        end
    end
    
    if closest then
        self:To(closest)
    else
        print("[Teleport] ไม่พบผู้เล่น")
    end
end

-- สร้างหน้าต่างรายชื่อ (Popup)
function Teleport:OpenWindow()
    if self.Window and self.Window.Parent then
        self.Window:Destroy()
    end
    
    -- หน้าต่างหลัก (กลางจอ)
    self.Window = Instance.new("Frame")
    self.Window.Size = UDim2.new(0, 280, 0, 350)
    self.Window.Position = UDim2.new(0.5, -140, 0.5, -175)
    self.Window.BackgroundColor3 = Color3.fromRGB(30, 30, 45)
    self.Window.BorderSizePixel = 0
    self.Window.Parent = sg
    
    local stroke = Instance.new("UIStroke", self.Window)
    stroke.Color = Color3.fromRGB(150, 100, 255)
    stroke.Thickness = 2
    
    -- หัวข้อ + ปุ่มปิด
    local header = Instance.new("Frame")
    header.Size = UDim2.new(1, 0, 0, 40)
    header.BackgroundColor3 = Color3.fromRGB(45, 45, 65)
    header.BorderSizePixel = 0
    header.Parent = self.Window
    
    local title = Instance.new("TextLabel", header)
    title.Size = UDim2.new(1, -50, 1, 0)
    title.Position = UDim2.new(0, 15, 0, 0)
    title.BackgroundTransparency = 1
    title.Text = "เลือกผู้เล่น"
    title.TextColor3 = Color3.fromRGB(200, 150, 255)
    title.Font = Enum.Font.GothamBold
    title.TextSize = 16
    title.TextXAlignment = Enum.TextXAlignment.Left
    
    local closeBtn = Instance.new("TextButton", header)
    closeBtn.Size = UDim2.new(0, 36, 0, 36)
    closeBtn.Position = UDim2.new(1, -40, 0, 2)
    closeBtn.BackgroundColor3 = Color3.fromRGB(200, 60, 60)
    closeBtn.Text = "×"
    closeBtn.TextColor3 = Color3.new(1, 1, 1)
    closeBtn.Font = Enum.Font.GothamBold
    closeBtn.TextSize = 20
    closeBtn.BorderSizePixel = 0
    
    closeBtn.MouseButton1Click:Connect(function()
        self.Window:Destroy()
        self.Enabled = false
        TeleportBtn.Text = "OPEN LIST"
        TeleportBtn.BackgroundColor3 = Color3.fromRGB(80, 80, 140)
    end)
    
    -- ScrollingList
    local scroll = Instance.new("ScrollingFrame", self.Window)
    scroll.Size = UDim2.new(1, -20, 1, -100)
    scroll.Position = UDim2.new(0, 10, 0, 50)
    scroll.BackgroundColor3 = Color3.fromRGB(25, 25, 35)
    scroll.BorderSizePixel = 0
    scroll.ScrollBarThickness = 6
    scroll.AutomaticCanvasSize = Enum.AutomaticSize.Y
    
    local listLayout = Instance.new("UIListLayout", scroll)
    listLayout.Padding = UDim.new(0, 5)
    listLayout.SortOrder = Enum.SortOrder.LayoutOrder
    
    -- สร้างปุ่มผู้เล่น
    local function refreshList()
        for _, btn in ipairs(self.ListButtons) do
            btn:Destroy()
        end
        self.ListButtons = {}
        
        for _, player in ipairs(Players:GetPlayers()) do
            if player ~= LocalPlayer then
                local btn = Instance.new("TextButton")
                btn.Size = UDim2.new(1, -10, 0, 32)
                btn.BackgroundColor3 = Color3.fromRGB(60, 60, 90)
                btn.Text = "  " .. player.Name
                btn.TextColor3 = Color3.new(1, 1, 1)
                btn.Font = Enum.Font.Gotham
                btn.TextSize = 14
                btn.TextXAlignment = Enum.TextXAlignment.Left
                btn.Parent = scroll
                
                btn.MouseEnter:Connect(function()
                    TweenService:Create(btn, TweenInfo.new(0.1), {BackgroundColor3 = Color3.fromRGB(80, 80, 120)}):Play()
                end)
                btn.MouseLeave:Connect(function()
                    TweenService:Create(btn, TweenInfo.new(0.1), {BackgroundColor3 = Color3.fromRGB(60, 60, 90)}):Play()
                end)
                
                btn.MouseButton1Click:Connect(function()
                    self:To(player)
                    btn.BackgroundColor3 = Color3.fromRGB(80, 180, 80)
                    btn.Text = "✓ " .. player.Name
                    task.wait(0.5)
                    btn.BackgroundColor3 = Color3.fromRGB(60, 60, 90)
                    btn.Text = "  " .. player.Name
                end)
                
                table.insert(self.ListButtons, btn)
            end
        end
    end
    
    refreshList()
    
    -- ปุ่มรีเฟรช + ใกล้ที่สุด
    local btnFrame = Instance.new("Frame", self.Window)
    btnFrame.Size = UDim2.new(1, -20, 0, 40)
    btnFrame.Position = UDim2.new(0, 10, 1, -50)
    btnFrame.BackgroundTransparency = 1
    
    local refreshBtn = Instance.new("TextButton", btnFrame)
    refreshBtn.Size = UDim2.new(0.48, -5, 1, 0)
    refreshBtn.BackgroundColor3 = Color3.fromRGB(80, 80, 140)
    refreshBtn.Text = "รีเฟรช"
    refreshBtn.TextColor3 = Color3.new(1, 1, 1)
    refreshBtn.Font = Enum.Font.GothamBold
    refreshBtn.TextSize = 13
    refreshBtn.BorderSizePixel = 0
    
    refreshBtn.MouseButton1Click:Connect(function()
        refreshList()
        refreshBtn.Text = "รีเฟรชแล้ว!"
        task.wait(0.5)
        refreshBtn.Text = "รีเฟรช"
    end)
    
    local closestBtn = Instance.new("TextButton", btnFrame)
    closestBtn.Size = UDim2.new(0.48, -5, 1, 0)
    closestBtn.Position = UDim2.new(0.52, 0, 0, 0)
    closestBtn.BackgroundColor3 = Color3.fromRGB(80, 140, 80)
    closestBtn.Text = "ใกล้ที่สุด"
    closestBtn.TextColor3 = Color3.new(1, 1, 1)
    closestBtn.Font = Enum.Font.GothamBold
    closestBtn.TextSize = 13
    closestBtn.BorderSizePixel = 0
    
    closestBtn.MouseButton1Click:Connect(function()
        self:ToClosest()
        closestBtn.Text = "เทเลพอร์ตแล้ว!"
        task.wait(0.5)
        closestBtn.Text = "ใกล้ที่สุด"
    end)
    
    -- อัพเดทอัตโนมัติเมื่อมีคนเข้า/ออก
    self.Connection = Players.PlayerAdded:Connect(refreshList)
    Players.PlayerRemoving:Connect(refreshList)
end

-- UI หลักในแท็บ Main (ปุ่มเปิด/ปิด)
local TPFrame = Instance.new("Frame")
TPFrame.Size = UDim2.new(1, -30, 0, 90)
TPFrame.BackgroundColor3 = Color3.fromRGB(35, 35, 48)
TPFrame.BorderSizePixel = 0
TPFrame.LayoutOrder = 8 -- ต่อจาก Aimbot
TPFrame.Parent = MainPage

local TPStroke = Instance.new("UIStroke", TPFrame)
TPStroke.Color = Color3.fromRGB(70, 70, 120)
TPStroke.Thickness = 1

local TPTitle = Instance.new("TextLabel")
TPTitle.Size = UDim2.new(1, -20, 0, 26)
TPTitle.Position = UDim2.new(0, 10, 0, 8)
TPTitle.BackgroundTransparency = 1
TPTitle.Text = "⚡ TELEPORT"
TPTitle.TextColor3 = Color3.fromRGB(150, 100, 255)
TPTitle.Font = Enum.Font.GothamBold
TPTitle.TextSize = 15
TPTitle.TextXAlignment = Enum.TextXAlignment.Left
TPTitle.Parent = TPFrame

local TPSubtitle = Instance.new("TextLabel")
TPSubtitle.Size = UDim2.new(1, -20, 0, 20)
TPSubtitle.Position = UDim2.new(0, 10, 0, 30)
TPSubtitle.BackgroundTransparency = 1
TPSubtitle.Text = "เลือกผู้เล่นแล้วเทเลพอร์ตไปหาทันที"
TPSubtitle.TextColor3 = Color3.fromRGB(180, 180, 220)
TPSubtitle.Font = Enum.Font.Gotham
TPSubtitle.TextSize = 11
TPSubtitle.TextXAlignment = Enum.TextXAlignment.Left
TPSubtitle.Parent = TPFrame

local TeleportBtn = Instance.new("TextButton")
TeleportBtn.Size = UDim2.new(0.9, 0, 0, 32)
TeleportBtn.Position = UDim2.new(0.05, 0, 0, 52)
TeleportBtn.BackgroundColor3 = Color3.fromRGB(80, 80, 140)
TeleportBtn.Text = "OPEN LIST"
TeleportBtn.TextColor3 = Color3.new(1, 1, 1)
TeleportBtn.Font = Enum.Font.GothamBold
TeleportBtn.TextSize = 14
TeleportBtn.BorderSizePixel = 0
TeleportBtn.Parent = TPFrame

-- Hover Effects
TeleportBtn.MouseEnter:Connect(function()
    TweenService:Create(TeleportBtn, TweenInfo.new(0.15), {BackgroundColor3 = Color3.fromRGB(100, 100, 170)}):Play()
end)
TeleportBtn.MouseLeave:Connect(function()
    local color = Teleport.Enabled and Color3.fromRGB(60, 160, 80) or Color3.fromRGB(80, 80, 140)
    TweenService:Create(TeleportBtn, TweenInfo.new(0.15), {BackgroundColor3 = color}):Play()
end)

-- กดเปิด/ปิด
TeleportBtn.MouseButton1Click:Connect(function()
    Teleport.Enabled = not Teleport.Enabled
    
    if Teleport.Enabled then
        TeleportBtn.Text = "CLOSE LIST"
        TeleportBtn.BackgroundColor3 = Color3.fromRGB(180, 60, 60)
        Teleport:OpenWindow()
    else
        TeleportBtn.Text = "OPEN LIST"
        TeleportBtn.BackgroundColor3 = Color3.fromRGB(80, 80, 140)
        if Teleport.Window then Teleport.Window:Destroy() end
    end
end)

-- คีย์ลัด (PC เท่านั้น) - T = ใกล้สุด
game:GetService("UserInputService").InputBegan:Connect(function(input, gameProcessed)
    if gameProcessed then return end
    if input.KeyCode == Enum.KeyCode.T then
        Teleport:ToClosest()
    end
end)

-- Cleanup
sg.Destroying:Connect(function()
    if Teleport.Connection then Teleport.Connection:Disconnect() end
    if Teleport.Window then Teleport.Window:Destroy() end
end)

print("[Teleport] Teleport System Loaded (PC: Press T for closest)")
-- ==============================================
-- INFINITE JUMP - กระโดดได้ไม่จำกัด (Mobile/PC)
-- ==============================================

local InfJump = {
    Enabled = false,
    Connection = nil,
    HighJump = false
}

function InfJump:Toggle()
    self.Enabled = not self.Enabled
    
    if self.Enabled then
        print("[InfJump] เปิดใช้งาน")
        
        -- ใช้ JumpRequest (ทำงานทั้ง PC และมือถือ)
        self.Connection = game:GetService("UserInputService").JumpRequest:Connect(function()
            local char = LocalPlayer.Character
            local hum = char and char:FindFirstChildOfClass("Humanoid")
            if hum then
                -- บังคับสถานะเป็น Jumping (กระโดดได้แม้อยู่กลางอากาศ)
                pcall(function()
                    hum:ChangeState(Enum.HumanoidStateType.Jumping)
                    if self.HighJump then
                        hum.JumpPower = 80
                    else
                        hum.JumpPower = 50
                    end
                end)
            end
        end)
    else
        print("[InfJump] ปิดใช้งาน")
        if self.Connection then
            self.Connection:Disconnect()
            self.Connection = nil
        end
        -- คืนค่า JumpPower
        local hum = LocalPlayer.Character and LocalPlayer.Character:FindFirstChildOfClass("Humanoid")
        if hum then hum.JumpPower = 50 end
    end
    
    return self.Enabled
end

-- UI (LayoutOrder 9 ต่อจาก Teleport)
local JumpFrame = Instance.new("Frame")
JumpFrame.Size = UDim2.new(1, -30, 0, 110)
JumpFrame.BackgroundColor3 = Color3.fromRGB(35, 35, 48)
JumpFrame.BorderSizePixel = 0
JumpFrame.LayoutOrder = 9
JumpFrame.Parent = MainPage

local JumpStroke = Instance.new("UIStroke", JumpFrame)
JumpStroke.Color = Color3.fromRGB(70, 70, 120)
JumpStroke.Thickness = 1

local JumpTitle = Instance.new("TextLabel")
JumpTitle.Size = UDim2.new(1, -20, 0, 26)
JumpTitle.Position = UDim2.new(0, 10, 0, 8)
JumpTitle.BackgroundTransparency = 1
JumpTitle.Text = "⬆️ INFINITE JUMP"
JumpTitle.TextColor3 = Color3.fromRGB(100, 200, 255)
JumpTitle.Font = Enum.Font.GothamBold
JumpTitle.TextSize = 15
JumpTitle.TextXAlignment = Enum.TextXAlignment.Left
JumpTitle.Parent = JumpFrame

local JumpSub = Instance.new("TextLabel")
JumpSub.Size = UDim2.new(1, -20, 0, 20)
JumpSub.Position = UDim2.new(0, 10, 0, 32)
JumpSub.BackgroundTransparency = 1
JumpSub.Text = "กระโดดซ้ำได้ในอากาศ (กดปุ่มกระโดด)"
JumpSub.TextColor3 = Color3.fromRGB(180, 180, 220)
JumpSub.Font = Enum.Font.Gotham
JumpSub.TextSize = 11
JumpSub.TextXAlignment = Enum.TextXAlignment.Left
JumpSub.Parent = JumpFrame

-- ปุ่มหลัก ON/OFF
local InfBtn = Instance.new("TextButton")
InfBtn.Size = UDim2.new(0.55, -5, 0, 36)
InfBtn.Position = UDim2.new(0, 10, 0, 60)
InfBtn.BackgroundColor3 = Color3.fromRGB(180, 60, 60)
InfBtn.Text = "OFF"
InfBtn.TextColor3 = Color3.new(1, 1, 1)
InfBtn.Font = Enum.Font.GothamBold
InfBtn.TextSize = 14
InfBtn.BorderSizePixel = 0
InfBtn.Parent = JumpFrame

-- ปุ่ม High Jump (อยู่ในเฟรมเดียวกัน ไม่ต้องสร้างใหม่)
local HighBtn = Instance.new("TextButton")
HighBtn.Size = UDim2.new(0.4, -5, 0, 36)
HighBtn.Position = UDim2.new(0.6, 0, 0, 60)
HighBtn.BackgroundColor3 = Color3.fromRGB(80, 80, 140)
HighBtn.Text = "High: OFF"
HighBtn.TextColor3 = Color3.new(1, 1, 1)
HighBtn.Font = Enum.Font.Gotham
HighBtn.TextSize = 12
HighBtn.BorderSizePixel = 0
HighBtn.Parent = JumpFrame

-- Hover Effects
InfBtn.MouseEnter:Connect(function()
    local color = InfJump.Enabled and Color3.fromRGB(80, 200, 100) or Color3.fromRGB(200, 80, 80)
    TweenService:Create(InfBtn, TweenInfo.new(0.15), {BackgroundColor3 = color}):Play()
end)
InfBtn.MouseLeave:Connect(function()
    local color = InfJump.Enabled and Color3.fromRGB(60, 160, 80) or Color3.fromRGB(180, 60, 60)
    TweenService:Create(InfBtn, TweenInfo.new(0.15), {BackgroundColor3 = color}):Play()
end)

HighBtn.MouseEnter:Connect(function()
    TweenService:Create(HighBtn, TweenInfo.new(0.15), {BackgroundColor3 = Color3.fromRGB(100, 100, 170)}):Play()
end)
HighBtn.MouseLeave:Connect(function()
    local color = InfJump.HighJump and Color3.fromRGB(100, 100, 170) or Color3.fromRGB(80, 80, 140)
    TweenService:Create(HighBtn, TweenInfo.new(0.15), {BackgroundColor3 = color}):Play()
end)

-- ฟังก์ชันกด
InfBtn.MouseButton1Click:Connect(function()
    local state = InfJump:Toggle()
    if state then
        InfBtn.Text = "ON"
        InfBtn.BackgroundColor3 = Color3.fromRGB(60, 160, 80)
        JumpTitle.TextColor3 = Color3.fromRGB(150, 255, 150)
    else
        InfBtn.Text = "OFF"
        InfBtn.BackgroundColor3 = Color3.fromRGB(180, 60, 60)
        JumpTitle.TextColor3 = Color3.fromRGB(100, 200, 255)
    end
end)

HighBtn.MouseButton1Click:Connect(function()
    InfJump.HighJump = not InfJump.HighJump
    if InfJump.HighJump then
        HighBtn.Text = "High: ON"
        HighBtn.BackgroundColor3 = Color3.fromRGB(80, 140, 80)
    else
        HighBtn.Text = "High: OFF"
        HighBtn.BackgroundColor3 = Color3.fromRGB(80, 80, 140)
        -- คืนค่าทันทีถ้าปิด
        local hum = LocalPlayer.Character and LocalPlayer.Character:FindFirstChildOfClass("Humanoid")
        if hum and InfJump.Enabled then hum.JumpPower = 50 end
    end
end)

-- Cleanup
sg.Destroying:Connect(function()
    InfJump.Enabled = false
    if InfJump.Connection then InfJump.Connection:Disconnect() end
    local hum = LocalPlayer.Character and LocalPlayer.Character:FindFirstChildOfClass("Humanoid")
    if hum then hum.JumpPower = 50 end
end)

print("[InfJump] Infinite Jump Loaded (Mobile/PC compatible)")
-- ==============================================
-- SOUND HUB - ระบบเสียง (กดเพื่อโหลด)
-- ==============================================

local SoundHub = {
    Loaded = false,
    GuiObject = nil,
    Connection = nil
}

function SoundHub:Unload()
    if self.GuiObject then
        pcall(function()
            self.GuiObject:Destroy()
        end)
        self.GuiObject = nil
    end
    
    if self.Connection then
        self.Connection:Disconnect()
        self.Connection = nil
    end
    
    self.Loaded = false
    print("[SoundHub] รีเซ็ตสถานะเรียบร้อย")
end


function SoundHub:Load()
    if self.Loaded then 
        print("[SoundHub] โหลดแล้วก่อนหน้านี้ กำลังรีเซ็ต...")
        self:Unload()
    end
    
    print("[SoundHub] กำลังโหลด Sound Hub...")
    local success, err = pcall(function()
        local result = loadstring(game:HttpGet("https://raw.githubusercontent.com/YONOOB490/orange-/refs/heads/main/SoundHub.lua"))()
        self.GuiObject = result
    end)
    
    if success then
        self.Loaded = true
        print("[SoundHub] โหลดสำเร็จ!")
        return true
    else
        warn("[SoundHub] โหลดไม่สำเร็จ:", err)
        self.Loaded = false
        return false, err
    end
end


-- UI Sound Hub (LayoutOrder 10 แทน Money Farm)
local SoundFrame = Instance.new("Frame")
SoundFrame.Size = UDim2.new(1, -30, 0, 100)
SoundFrame.BackgroundColor3 = Color3.fromRGB(35, 35, 48)
SoundFrame.BorderSizePixel = 0
SoundFrame.LayoutOrder = 10
SoundFrame.Parent = MainPage

local SoundStroke = Instance.new("UIStroke", SoundFrame)
SoundStroke.Color = Color3.fromRGB(70, 70, 120)
SoundStroke.Thickness = 1

local SoundTitle = Instance.new("TextLabel")
SoundTitle.Size = UDim2.new(1, -20, 0, 26)
SoundTitle.Position = UDim2.new(0, 10, 0, 8)
SoundTitle.BackgroundTransparency = 1
SoundTitle.Text = "🔊 SOUND HUB"
SoundTitle.TextColor3 = Color3.fromRGB(255, 150, 50)
SoundTitle.Font = Enum.Font.GothamBold
SoundTitle.TextSize = 15
SoundTitle.TextXAlignment = Enum.TextXAlignment.Left
SoundTitle.Parent = SoundFrame

local SoundSub = Instance.new("TextLabel")
SoundSub.Size = UDim2.new(1, -20, 0, 20)
SoundSub.Position = UDim2.new(0, 10, 0, 32)
SoundSub.BackgroundTransparency = 1
SoundSub.Text = "โหลดระบบเสียงและเพลง"
SoundSub.TextColor3 = Color3.fromRGB(180, 180, 220)
SoundSub.Font = Enum.Font.Gotham
SoundSub.TextSize = 11
SoundSub.TextXAlignment = Enum.TextXAlignment.Left
SoundSub.Parent = SoundFrame

local LoadBtn = Instance.new("TextButton")
LoadBtn.Size = UDim2.new(0.9, 0, 0, 36)
LoadBtn.Position = UDim2.new(0.05, 0, 0, 58)
LoadBtn.BackgroundColor3 = Color3.fromRGB(80, 80, 140)
LoadBtn.Text = "LOAD SOUND HUB"
LoadBtn.TextColor3 = Color3.new(1, 1, 1)
LoadBtn.Font = Enum.Font.GothamBold
LoadBtn.TextSize = 14
LoadBtn.BorderSizePixel = 0
LoadBtn.Parent = SoundFrame

-- Hover Effects
LoadBtn.MouseEnter:Connect(function()
    TweenService:Create(LoadBtn, TweenInfo.new(0.15), {BackgroundColor3 = Color3.fromRGB(100, 100, 170)}):Play()
end)
LoadBtn.MouseLeave:Connect(function()
    local color = SoundHub.Loaded and Color3.fromRGB(60, 160, 80) or Color3.fromRGB(80, 80, 140)
    TweenService:Create(LoadBtn, TweenInfo.new(0.15), {BackgroundColor3 = color}):Play()
end)

-- กดโหลด
LoadBtn.MouseButton1Click:Connect(function()
    if SoundHub.Loaded then
        LoadBtn.Text = "UNLOADING..."
        LoadBtn.BackgroundColor3 = Color3.fromRGB(100, 100, 100)
        
        SoundHub:Unload()
        
        LoadBtn.Text = "LOAD SOUND HUB"
        LoadBtn.BackgroundColor3 = Color3.fromRGB(80, 80, 140)
        SoundTitle.TextColor3 = Color3.fromRGB(255, 150, 50)
        SoundSub.Text = "โหลดระบบเสียงและเพลง"
        return
    end
    
    LoadBtn.Text = "LOADING..."
    LoadBtn.BackgroundColor3 = Color3.fromRGB(100, 100, 100)
    
    local success, err = SoundHub:Load()
    
    if success then
        LoadBtn.Text = "LOADED ✓"
        LoadBtn.BackgroundColor3 = Color3.fromRGB(60, 160, 80)
        SoundTitle.TextColor3 = Color3.fromRGB(150, 255, 150)
        SoundSub.Text = "พร้อมใช้งาน - กดอีกครั้งเพื่อปิด"
    else
        LoadBtn.Text = "ERROR! Retry"
        LoadBtn.BackgroundColor3 = Color3.fromRGB(180, 60, 60)
        SoundSub.Text = "Error: " .. tostring(err):sub(1, 30)
        
        task.wait(2)
        LoadBtn.Text = "LOAD SOUND HUB"
        LoadBtn.BackgroundColor3 = Color3.fromRGB(80, 80, 140)
        SoundSub.Text = "โหลดระบบเสียงและเพลง"
    end
end)

print("[SoundHub] Sound Hub Button Ready (Layout 10)")
-- ==============================================
-- FULL BRIGHT - สว่างทั้งแมพ (ลบเงา ลบความมืด)
-- ==============================================

local FullBright = {
    Enabled = false,
    Connection = nil,
    Original = {} -- เก็บค่าเดิม
}

function FullBright:SaveOriginal()
    local lighting = game:GetService("Lighting")
    self.Original = {
        Brightness = lighting.Brightness,
        ClockTime = lighting.ClockTime,
        FogEnd = lighting.FogEnd,
        GlobalShadows = lighting.GlobalShadows,
        Ambient = lighting.Ambient,
        OutdoorAmbient = lighting.OutdoorAmbient
    }
end

function FullBright:Apply()
    local lighting = game:GetService("Lighting")
    
    -- บันทึกค่าเดิมถ้ายังไม่มี
    if not self.Original.Brightness then
        self:SaveOriginal()
    end
    
    -- ปรับให้สว่างสุดๆ
    lighting.Brightness = 10
    lighting.ClockTime = 12 -- เที่ยงวัน
    lighting.FogEnd = 100000 -- ลบหมอก
    lighting.GlobalShadows = false -- ลบเงา
    lighting.Ambient = Color3.fromRGB(255, 255, 255) -- แสงรอบข้างขาวสุด
    lighting.OutdoorAmbient = Color3.fromRGB(255, 255, 255)
end

function FullBright:Restore()
    local lighting = game:GetService("Lighting")
    if self.Original.Brightness then
        lighting.Brightness = self.Original.Brightness
        lighting.ClockTime = self.Original.ClockTime
        lighting.FogEnd = self.Original.FogEnd
        lighting.GlobalShadows = self.Original.GlobalShadows
        lighting.Ambient = self.Original.Ambient
        lighting.OutdoorAmbient = self.Original.OutdoorAmbient
    end
end

function FullBright:Toggle()
    self.Enabled = not self.Enabled
    
    if self.Enabled then
        print("[FullBright] เปิดใช้งาน")
        self:Apply()
        
        -- Loop คอยดันไม่ให้เกมเปลี่ยนกลับ (บางเกมมีระบบวัน/คืน)
        self.Connection = game:GetService("RunService").Heartbeat:Connect(function()
            if self.Enabled then
                local lighting = game:GetService("Lighting")
                -- ถ้าเกมพยายามเปลี่ยน ให้ดันกลับ
                if lighting.Brightness < 5 then
                    lighting.Brightness = 10
                end
                if lighting.GlobalShadows then
                    lighting.GlobalShadows = false
                end
            end
        end)
        
    else
        print("[FullBright] ปิดใช้งาน")
        if self.Connection then
            self.Connection:Disconnect()
            self.Connection = nil
        end
        self:Restore()
    end
    
    return self.Enabled
end

-- UI Full Bright (LayoutOrder 11 ต่อจาก Sound Hub)
local BrightFrame = Instance.new("Frame")
BrightFrame.Size = UDim2.new(1, -30, 0, 90)
BrightFrame.BackgroundColor3 = Color3.fromRGB(35, 35, 48)
BrightFrame.BorderSizePixel = 0
BrightFrame.LayoutOrder = 11
BrightFrame.Parent = MainPage

local BrightStroke = Instance.new("UIStroke", BrightFrame)
BrightStroke.Color = Color3.fromRGB(70, 70, 120)
BrightStroke.Thickness = 1

local BrightTitle = Instance.new("TextLabel")
BrightTitle.Size = UDim2.new(1, -20, 0, 26)
BrightTitle.Position = UDim2.new(0, 10, 0, 8)
BrightTitle.BackgroundTransparency = 1
BrightTitle.Text = "☀️ FULL BRIGHT"
BrightTitle.TextColor3 = Color3.fromRGB(255, 255, 100)
BrightTitle.Font = Enum.Font.GothamBold
BrightTitle.TextSize = 15
BrightTitle.TextXAlignment = Enum.TextXAlignment.Left
BrightTitle.Parent = BrightFrame

local BrightSub = Instance.new("TextLabel")
BrightSub.Size = UDim2.new(1, -20, 0, 20)
BrightSub.Position = UDim2.new(0, 10, 0, 32)
BrightSub.BackgroundTransparency = 1
BrightSub.Text = "ลบความมืด ลบเงา สว่างทั้งแมพ"
BrightSub.TextColor3 = Color3.fromRGB(180, 180, 220)
BrightSub.Font = Enum.Font.Gotham
BrightSub.TextSize = 11
BrightSub.TextXAlignment = Enum.TextXAlignment.Left
BrightSub.Parent = BrightFrame

local BrightBtn = Instance.new("TextButton")
BrightBtn.Size = UDim2.new(0.9, 0, 0, 32)
BrightBtn.Position = UDim2.new(0.05, 0, 0, 52)
BrightBtn.BackgroundColor3 = Color3.fromRGB(180, 60, 60)
BrightBtn.Text = "OFF"
BrightBtn.TextColor3 = Color3.new(1, 1, 1)
BrightBtn.Font = Enum.Font.GothamBold
BrightBtn.TextSize = 14
BrightBtn.BorderSizePixel = 0
BrightBtn.Parent = BrightFrame

-- Hover Effects
BrightBtn.MouseEnter:Connect(function()
    local color = FullBright.Enabled and Color3.fromRGB(80, 200, 100) or Color3.fromRGB(200, 80, 80)
    TweenService:Create(BrightBtn, TweenInfo.new(0.15), {BackgroundColor3 = color}):Play()
end)
BrightBtn.MouseLeave:Connect(function()
    local color = FullBright.Enabled and Color3.fromRGB(60, 160, 80) or Color3.fromRGB(180, 60, 60)
    TweenService:Create(BrightBtn, TweenInfo.new(0.15), {BackgroundColor3 = color}):Play()
end)

-- กดปุ่ม
BrightBtn.MouseButton1Click:Connect(function()
    local state = FullBright:Toggle()
    if state then
        BrightBtn.Text = "ON"
        BrightBtn.BackgroundColor3 = Color3.fromRGB(60, 160, 80)
        BrightTitle.TextColor3 = Color3.fromRGB(150, 255, 150)
    else
        BrightBtn.Text = "OFF"
        BrightBtn.BackgroundColor3 = Color3.fromRGB(180, 60, 60)
        BrightTitle.TextColor3 = Color3.fromRGB(255, 255, 100)
    end
end)

-- Cleanup เมื่อปิด GUI
sg.Destroying:Connect(function()
    FullBright:Restore()
end)

print("[FullBright] Full Bright Loaded (Layout 11)")
-- ==============================================
-- AUTO CLICKER - คลิก/ตีอัตโนมัติ (ใช้ได้จริง)
-- ==============================================

local Clicker = {
    Enabled = false,
    Speed = 10, -- ครั้งต่อวินาที
    Mode = "Tool", -- "Tool" หรือ "Mouse"
    Connection = nil,
    LastClick = 0
}

function Clicker:Click()
    if self.Mode == "Tool" then
        -- ใช้ Tool (เหมาะกับเกมตี/ฟัน)
        local char = LocalPlayer.Character
        local tool = char and char:FindFirstChildOfClass("Tool")
        if tool then
            pcall(function()
                tool:Activate()
            end)
        end
    else
        -- ใช้ Mouse (PC เท่านั้น)
        local mouse = LocalPlayer:GetMouse()
        if mouse then
            pcall(function()
                mouse1click()
            end)
        end
    end
end

function Clicker:Update()
    if not self.Enabled then return end
    
    local now = tick()
    local interval = 1 / self.Speed
    
    if now - self.LastClick >= interval then
        self.LastClick = now
        self:Click()
    end
end

function Clicker:Toggle()
    self.Enabled = not self.Enabled
    
    if self.Enabled then
        print("[Clicker] เปิดใช้งาน (" .. self.Speed .. " CPS)")
        
        -- ใช้ Heartbeat (ไม่ควรมี task.wait ข้างใน)
        self.Connection = game:GetService("RunService").Heartbeat:Connect(function()
            self:Update()
        end)
    else
        print("[Clicker] ปิดใช้งาน")
        if self.Connection then
            self.Connection:Disconnect()
            self.Connection = nil
        end
    end
    
    return self.Enabled
end

-- UI (LayoutOrder 12 ต่อจาก Full Bright)
local ClickerFrame = Instance.new("Frame")
ClickerFrame.Size = UDim2.new(1, -30, 0, 120)
ClickerFrame.BackgroundColor3 = Color3.fromRGB(35, 35, 48)
ClickerFrame.BorderSizePixel = 0
ClickerFrame.LayoutOrder = 12
ClickerFrame.Parent = MainPage

local ClickerStroke = Instance.new("UIStroke", ClickerFrame)
ClickerStroke.Color = Color3.fromRGB(70, 70, 120)
ClickerStroke.Thickness = 1

local ClickerTitle = Instance.new("TextLabel")
ClickerTitle.Size = UDim2.new(1, -20, 0, 26)
ClickerTitle.Position = UDim2.new(0, 10, 0, 8)
ClickerTitle.BackgroundTransparency = 1
ClickerTitle.Text = "⚡ AUTO CLICKER"
ClickerTitle.TextColor3 = Color3.fromRGB(100, 255, 100)
ClickerTitle.Font = Enum.Font.GothamBold
ClickerTitle.TextSize = 15
ClickerTitle.TextXAlignment = Enum.TextXAlignment.Left
ClickerTitle.Parent = ClickerFrame

local ClickerSub = Instance.new("TextLabel")
ClickerSub.Size = UDim2.new(1, -20, 0, 20)
ClickerSub.Position = UDim2.new(0, 10, 0, 32)
ClickerSub.BackgroundTransparency = 1
ClickerSub.Text = "โหมด Tool (เหมาะกับเกมตี/ฟัน)"
ClickerSub.TextColor3 = Color3.fromRGB(180, 180, 220)
ClickerSub.Font = Enum.Font.Gotham
ClickerSub.TextSize = 11
ClickerSub.TextXAlignment = Enum.TextXAlignment.Left
ClickerSub.Parent = ClickerFrame

-- ปุ่ม Toggle
local ClickerBtn = Instance.new("TextButton")
ClickerBtn.Size = UDim2.new(0.35, 0, 0, 36)
ClickerBtn.Position = UDim2.new(0, 10, 0, 60)
ClickerBtn.BackgroundColor3 = Color3.fromRGB(180, 60, 60)
ClickerBtn.Text = "OFF"
ClickerBtn.TextColor3 = Color3.new(1, 1, 1)
ClickerBtn.Font = Enum.Font.GothamBold
ClickerBtn.TextSize = 14
ClickerBtn.BorderSizePixel = 0
ClickerBtn.Parent = ClickerFrame

-- ปุ่มลดความเร็ว
local ClickerSlowBtn = Instance.new("TextButton")
ClickerSlowBtn.Size = UDim2.new(0.15, 0, 0, 36)
ClickerSlowBtn.Position = UDim2.new(0.4, 0, 0, 60)
ClickerSlowBtn.BackgroundColor3 = Color3.fromRGB(80, 80, 140)
ClickerSlowBtn.Text = "-"
ClickerSlowBtn.TextColor3 = Color3.new(1, 1, 1)
ClickerSlowBtn.Font = Enum.Font.GothamBold
ClickerSlowBtn.TextSize = 18
ClickerSlowBtn.BorderSizePixel = 0
ClickerSlowBtn.Parent = ClickerFrame

-- แสดงความเร็ว
local ClickerSpeedDisplay = Instance.new("TextLabel")
ClickerSpeedDisplay.Size = UDim2.new(0.15, 0, 0, 36)
ClickerSpeedDisplay.Position = UDim2.new(0.57, 0, 0, 60)
ClickerSpeedDisplay.BackgroundColor3 = Color3.fromRGB(50, 50, 70)
ClickerSpeedDisplay.Text = tostring(Clicker.Speed)
ClickerSpeedDisplay.TextColor3 = Color3.new(1, 1, 1)
ClickerSpeedDisplay.Font = Enum.Font.GothamBold
ClickerSpeedDisplay.TextSize = 14
ClickerSpeedDisplay.Parent = ClickerFrame

-- ปุ่มเพิ่มความเร็ว
local ClickerFastBtn = Instance.new("TextButton")
ClickerFastBtn.Size = UDim2.new(0.15, 0, 0, 36)
ClickerFastBtn.Position = UDim2.new(0.75, 0, 0, 60)
ClickerFastBtn.BackgroundColor3 = Color3.fromRGB(80, 140, 80)
ClickerFastBtn.Text = "+"
ClickerFastBtn.TextColor3 = Color3.new(1, 1, 1)
ClickerFastBtn.Font = Enum.Font.GothamBold
ClickerFastBtn.TextSize = 18
ClickerFastBtn.BorderSizePixel = 0
ClickerFastBtn.Parent = ClickerFrame

-- Hover Effects
ClickerBtn.MouseEnter:Connect(function()
    local color = Clicker.Enabled and Color3.fromRGB(80, 200, 100) or Color3.fromRGB(200, 80, 80)
    TweenService:Create(ClickerBtn, TweenInfo.new(0.15), {BackgroundColor3 = color}):Play()
end)
ClickerBtn.MouseLeave:Connect(function()
    local color = Clicker.Enabled and Color3.fromRGB(60, 160, 80) or Color3.fromRGB(180, 60, 60)
    TweenService:Create(ClickerBtn, TweenInfo.new(0.15), {BackgroundColor3 = color}):Play()
end)

-- กด Toggle
ClickerBtn.MouseButton1Click:Connect(function()
    local state = Clicker:Toggle()
    if state then
        ClickerBtn.Text = "ON"
        ClickerBtn.BackgroundColor3 = Color3.fromRGB(60, 160, 80)
        ClickerTitle.TextColor3 = Color3.fromRGB(150, 255, 150)
    else
        ClickerBtn.Text = "OFF"
        ClickerBtn.BackgroundColor3 = Color3.fromRGB(180, 60, 60)
        ClickerTitle.TextColor3 = Color3.fromRGB(100, 255, 100)
    end
end)

-- ปรับความเร็ว
ClickerSlowBtn.MouseButton1Click:Connect(function()
    Clicker.Speed = math.max(1, Clicker.Speed - 1)
    ClickerSpeedDisplay.Text = tostring(Clicker.Speed)
end)

ClickerFastBtn.MouseButton1Click:Connect(function()
    Clicker.Speed = math.min(30, Clicker.Speed + 1)
    ClickerSpeedDisplay.Text = tostring(Clicker.Speed)
end)

-- Hover ปุ่ม +/- 
ClickerSlowBtn.MouseEnter:Connect(function()
    TweenService:Create(ClickerSlowBtn, TweenInfo.new(0.15), {BackgroundColor3 = Color3.fromRGB(100, 100, 170)}):Play()
end)
ClickerSlowBtn.MouseLeave:Connect(function()
    TweenService:Create(ClickerSlowBtn, TweenInfo.new(0.15), {BackgroundColor3 = Color3.fromRGB(80, 80, 140)}):Play()
end)

ClickerFastBtn.MouseEnter:Connect(function()
    TweenService:Create(ClickerFastBtn, TweenInfo.new(0.15), {BackgroundColor3 = Color3.fromRGB(100, 170, 100)}):Play()
end)
ClickerFastBtn.MouseLeave:Connect(function()
    TweenService:Create(ClickerFastBtn, TweenInfo.new(0.15), {BackgroundColor3 = Color3.fromRGB(80, 140, 80)}):Play()
end)

-- Cleanup
sg.Destroying:Connect(function()
    Clicker.Enabled = false
    if Clicker.Connection then Clicker.Connection:Disconnect() end
end)

print("[Clicker] Auto Clicker Loaded (Layout 12)")
print("[Clicker] Mode: Tool Activate | Max: 30 CPS")
-- ==============================================
-- NPC WARP AI v3.0 - ตรวจจับ NPC/Dummy อัตโนมัติ
-- ==============================================

local NPCWarp_AI = {
    Enabled = false,
    Mode = "Interact", -- Interact / Attack / Follow
    Range = 200,
    Connection = nil,
    LastScan = 0,
    ScanDelay = 1.5, -- สแกนทุก 1.5 วินาที (ประหยัด FPS)
    CurrentTarget = nil,
    
    -- เกณฑ์ตัดสินใจ AI
    Criteria = {
        MinHealth = 10,
        MaxHealth = 10000,
        MaxWalkSpeed = 100,
    }
}

function NPCWarp_AI:IsNPC(obj)
    -- เร็วสุด: ตรวจสอบพื้นฐาน
    if not obj or not obj.Parent then return false end
    if not obj:IsA("Model") then return false end
    
    -- เร็ว: ตรวจสอบผู้เล่น (ถ้าเป็นผู้เล่นจริง ไม่ใช่ NPC)
    if game.Players:GetPlayerFromCharacter(obj) then return false end
    
    -- ตรวจสอบ Humanoid
    local humanoid = nil
    pcall(function() humanoid = obj:FindFirstChildOfClass("Humanoid") end)
    if not humanoid then return false end
    
    -- ตรวจสอบสุขภาพอยู่ในช่วงที่เป็น NPC ได้
    if humanoid.Health < self.Criteria.MinHealth or humanoid.Health > self.Criteria.MaxHealth then
        return false
    end
    
    -- ตรวจสอบความเร็ว (NPC มักไม่เร็วมาก)
    if humanoid.WalkSpeed > self.Criteria.MaxWalkSpeed then
        return false
    end
    
    -- ตรวจสอบ Animator (NPC มี Animator แต่ไม่มี PlayerScripts)
    local hasAnimator = false
    pcall(function()
        if obj:FindFirstChild("Animator") then
            hasAnimator = true
        end
    end)
    
    if hasAnimator and not obj:FindFirstChildOfClass("PlayerScripts") then
        return true
    end
    
    -- ตรวจสอบ ProximityPrompt หรือ Dialog (NPC มักมี)
    pcall(function()
        if obj:FindFirstChildOfClass("ProximityPrompt") or 
           obj:FindFirstChildOfClass("Dialog") then
            return true
        end
    end)
    
    -- ถ้ามี Humanoid และไม่ใช่ผู้เล่น มีโอกาสเป็น NPC สูง
    return true
end

function NPCWarp_AI:FindNearest()
    if not LocalPlayer.Character then return nil end
    local hrp = LocalPlayer.Character:FindFirstChild("HumanoidRootPart")
    if not hrp then return nil end
    
    local charPos = hrp.Position
    local nearest = nil
    local minDist = self.Range
    
    -- สแกนเฉพาะทุก 1.5 วินาที
    if tick() - self.LastScan < self.ScanDelay then return self.CurrentTarget end
    self.LastScan = tick()
    
    local function scan(folder)
        pcall(function()
            for _, child in ipairs(folder:GetChildren()) do
                if child:IsA("Model") and child ~= LocalPlayer.Character then
                    if self:IsNPC(child) then
                        local part = child:FindFirstChild("HumanoidRootPart") or 
                                   child:FindFirstChild("Torso") or 
                                   child:FindFirstChild("UpperTorso")
                        if part then
                            local dist = (charPos - part.Position).Magnitude
                            if dist < minDist then
                                minDist = dist
                                nearest = child
                            end
                        end
                    end
                end
                
                -- สแกนลึกลงไปแต่ไม่เกิน 2 ชั้น (ป้องกันแลค)
                if child:IsA("Folder") or (child:IsA("Model") and #child:GetChildren() < 20) then
                    scan(child)
                end
            end
        end)
    end
    
    scan(workspace)
    return nearest
end

function NPCWarp_AI:Warp(npc)
    if not npc then return end
    local char = LocalPlayer.Character
    if not char then return end
    
    local hrp = char:FindFirstChild("HumanoidRootPart")
    local npcHRP = nil
    
    pcall(function()
        npcHRP = npc:FindFirstChild("HumanoidRootPart") or 
               npc:FindFirstChild("Torso") or 
               npc:FindFirstChild("UpperTorso")
    end)
    
    if not hrp or not npcHRP then return end
    
    -- วาร์ปไปด้านหน้า 4 studs
    local destination = npcHRP.CFrame * CFrame.new(0, 0, -4) + Vector3.new(0, 2, 0)
    pcall(function()
        hrp.CFrame = destination
        hrp.Velocity = Vector3.new(0, 0, 0)
    end)
    
    self.CurrentTarget = npc
    
    -- Auto Interact/Attack
    task.wait(0.2)
    if self.Mode == "Interact" then
        pcall(function()
            local prompt = npc:FindFirstChildOfClass("ProximityPrompt")
            if prompt then
                prompt:InputHoldBegin()
                task.wait(0.1)
                prompt:InputHoldEnd()
            end
            local click = npc:FindFirstChildOfClass("ClickDetector")
            if click and fireclickdetector then
                fireclickdetector(click)
            end
        end)
    elseif self.Mode == "Attack" then
        local tool = char:FindFirstChildOfClass("Tool")
        if tool then tool:Activate() end
    end
end

function NPCWarp_AI:Update()
    if not self.Enabled or not LocalPlayer.Character then return end
    
    local hrp = LocalPlayer.Character:FindFirstChild("HumanoidRootPart")
    if not hrp then return end
    
    local target = self:FindNearest()
    if target then
        local npcHRP = target:FindFirstChild("HumanoidRootPart")
        if npcHRP then
            local dist = (hrp.Position - npcHRP.Position).Magnitude
            if dist > 8 then
                self:Warp(target)
            elseif self.Mode == "Attack" then
                local tool = LocalPlayer.Character:FindFirstChildOfClass("Tool")
                if tool then tool:Activate() end
            end
        end
    end
end

function NPCWarp_AI:Toggle()
    self.Enabled = not self.Enabled
    
    if self.Enabled then
        print("[NPC AI] เปิดใช้งาน - Auto Detect Mode")
        print("[NPC AI] สแกนทุก " .. self.ScanDelay .. " วินาที")
        self.Connection = game:GetService("RunService").Heartbeat:Connect(function()
            self:Update()
        end)
    else
        print("[NPC AI] ปิดใช้งาน")
        if self.Connection then
            self.Connection:Disconnect()
            self.Connection = nil
        end
        self.CurrentTarget = nil
    end
    
    return self.Enabled
end

-- ==============================================
-- UI NPC Warp AI (เรียบง่าย ใช้งานง่าย)
-- ==============================================

local NPCAI_Frame = Instance.new("Frame")
NPCAI_Frame.Size = UDim2.new(1, -30, 0, 90)
NPCAI_Frame.BackgroundColor3 = Color3.fromRGB(35, 35, 48)
NPCAI_Frame.BorderSizePixel = 0
NPCAI_Frame.LayoutOrder = 13
NPCAI_Frame.Parent = MainPage

local NPCAI_Stroke = Instance.new("UIStroke", NPCAI_Frame)
NPCAI_Stroke.Color = Color3.fromRGB(70, 70, 120)
NPCAI_Stroke.Thickness = 1

local NPCAI_Title = Instance.new("TextLabel")
NPCAI_Title.Size = UDim2.new(1, -20, 0, 24)
NPCAI_Title.Position = UDim2.new(0, 10, 0, 8)
NPCAI_Title.BackgroundTransparency = 1
NPCAI_Title.Text = "🤖 NPC WARP"
NPCAI_Title.TextColor3 = Color3.fromRGB(100, 200, 255)
NPCAI_Title.Font = Enum.Font.GothamBold
NPCAI_Title.TextSize = 15
NPCAI_Title.TextXAlignment = Enum.TextXAlignment.Left
NPCAI_Title.Parent = NPCAI_Frame

local NPCAI_Status = Instance.new("TextLabel")
NPCAI_Status.Size = UDim2.new(1, -20, 0, 20)
NPCAI_Status.Position = UDim2.new(0, 10, 0, 32)
NPCAI_Status.BackgroundTransparency = 1
NPCAI_Status.Text = "พร้อมตรวจจับ NPC/Dummy อัตโนมัติ"
NPCAI_Status.TextColor3 = Color3.fromRGB(180, 180, 220)
NPCAI_Status.Font = Enum.Font.Gotham
NPCAI_Status.TextSize = 11
NPCAI_Status.TextXAlignment = Enum.TextXAlignment.Left
NPCAI_Status.Parent = NPCAI_Frame

local NPCAI_ToggleBtn = Instance.new("TextButton")
NPCAI_ToggleBtn.Size = UDim2.new(0.45, 0, 0, 32)
NPCAI_ToggleBtn.Position = UDim2.new(0, 10, 0, 54)
NPCAI_ToggleBtn.BackgroundColor3 = Color3.fromRGB(180, 60, 60)
NPCAI_ToggleBtn.Text = "OFF"
NPCAI_ToggleBtn.TextColor3 = Color3.new(1, 1, 1)
NPCAI_ToggleBtn.Font = Enum.Font.GothamBold
NPCAI_ToggleBtn.TextSize = 14
NPCAI_ToggleBtn.BorderSizePixel = 0
NPCAI_ToggleBtn.Parent = NPCAI_Frame

local NPCAI_ModeBtn = Instance.new("TextButton")
NPCAI_ModeBtn.Size = UDim2.new(0.48, 0, 0, 32)
NPCAI_ModeBtn.Position = UDim2.new(0.52, 0, 0, 54)
NPCAI_ModeBtn.BackgroundColor3 = Color3.fromRGB(80, 80, 140)
NPCAI_ModeBtn.Text = "Mode: Interact"
NPCAI_ModeBtn.TextColor3 = Color3.new(1, 1, 1)
NPCAI_ModeBtn.Font = Enum.Font.Gotham
NPCAI_ModeBtn.TextSize = 12
NPCAI_ModeBtn.BorderSizePixel = 0
NPCAI_ModeBtn.Parent = NPCAI_Frame

-- Hover Effects
NPCAI_ToggleBtn.MouseEnter:Connect(function()
    local color = NPCWarp_AI.Enabled and Color3.fromRGB(80, 200, 100) or Color3.fromRGB(200, 80, 80)
    TweenService:Create(NPCAI_ToggleBtn, TweenInfo.new(0.15), {BackgroundColor3 = color}):Play()
end)

NPCAI_ToggleBtn.MouseLeave:Connect(function()
    local color = NPCWarp_AI.Enabled and Color3.fromRGB(60, 160, 80) or Color3.fromRGB(180, 60, 60)
    TweenService:Create(NPCAI_ToggleBtn, TweenInfo.new(0.15), {BackgroundColor3 = color}):Play()
end)

-- Toggle
NPCAI_ToggleBtn.MouseButton1Click:Connect(function()
    local state = NPCWarp_AI:Toggle()
    if state then
        NPCAI_ToggleBtn.Text = "ON"
        NPCAI_ToggleBtn.BackgroundColor3 = Color3.fromRGB(60, 160, 80)
        NPCAI_Title.TextColor3 = Color3.fromRGB(150, 255, 150)
        NPCAI_Status.Text = "กำลังตรวจจับ NPC อัตโนมัติ..."
        NPCAI_Status.TextColor3 = Color3.fromRGB(100, 255, 100)
    else
        NPCAI_ToggleBtn.Text = "OFF"
        NPCAI_ToggleBtn.BackgroundColor3 = Color3.fromRGB(180, 60, 60)
        NPCAI_Title.TextColor3 = Color3.fromRGB(100, 200, 255)
        NPCAI_Status.Text = "พร้อมตรวจจับ NPC/Dummy อัตโนมัติ"
        NPCAI_Status.TextColor3 = Color3.fromRGB(180, 180, 220)
    end
end)

-- Mode Switch
NPCAI_ModeBtn.MouseButton1Click:Connect(function()
    local modes = {"Interact", "Attack", "Follow"}
    local idx = table.find(modes, NPCWarp_AI.Mode) or 1
    idx = idx % #modes + 1
    NPCWarp_AI.Mode = modes[idx]
    NPCAI_ModeBtn.Text = "Mode: " .. NPCWarp_AI.Mode
    
    print("[NPC AI] เปลี่ยนโหมดเป็น: " .. NPCWarp_AI.Mode)
end)

NPCAI_ModeBtn.MouseEnter:Connect(function()
    TweenService:Create(NPCAI_ModeBtn, TweenInfo.new(0.15), {BackgroundColor3 = Color3.fromRGB(100, 100, 170)}):Play()
end)
NPCAI_ModeBtn.MouseLeave:Connect(function()
    TweenService:Create(NPCAI_ModeBtn, TweenInfo.new(0.15), {BackgroundColor3 = Color3.fromRGB(80, 80, 140)}):Play()
end)

-- Cleanup
sg.Destroying:Connect(function()
    NPCWarp_AI.Enabled = false
    if NPCWarp_AI.Connection then NPCWarp_AI.Connection:Disconnect() end
end)

print("[NPC AI] NPC Warp AI v3.0 Loaded - Auto Detect Mode")
print("[NPC AI] ไม่ต้องระบุชื่อ สแกนทุก 1.5 วินาที")

-- ==============================================
-- HITBOX EXPANDER - ขยายพื้นที่โจมตี (Layout 14)
-- ==============================================

local Hitbox = {
    Enabled = false,
    Size = 3, -- ขนาดคูณ (3 = 3x)
    Connection = nil,
    LastScan = 0,
    Originals = {} -- [Part] = {Size=..., Trans=..., Mat=...}
}

function Hitbox:ProcessCharacter(char)
    if not char then return end
    local isEnemy = false
    
    -- เช็คว่าเป็นศัตรู (ไม่ใช่ทีมเดียวกัน)
    for _, player in ipairs(Players:GetPlayers()) do
        if player.Character == char and player ~= LocalPlayer then
            if LocalPlayer.Team and player.Team and LocalPlayer.Team ~= player.Team then
                isEnemy = true
            elseif not LocalPlayer.Team then
                isEnemy = true
            end
            break
        end
    end
    
    if not isEnemy then return end
    
    -- ขยายส่วนสำคัญ
    local targets = {"Head", "HumanoidRootPart", "UpperTorso", "Torso"}
    for _, name in ipairs(targets) do
        local part = char:FindFirstChild(name)
        if part and part:IsA("BasePart") then
            if not self.Originals[part] then
                self.Originals[part] = {
                    Size = part.Size,
                    Transparency = part.Transparency,
                    Material = part.Material
                }
            end
            
            if self.Enabled then
                -- ขยายและทำให้เห็นได้ชัด
                part.Size = self.Originals[part].Size * self.Size
                part.Transparency = 0.7
                part.Material = Enum.Material.Neon
            else
                -- คืนค่า
                part.Size = self.Originals[part].Size
                part.Transparency = self.Originals[part].Transparency
                part.Material = self.Originals[part].Material
            end
        end
    end
end

function Hitbox:Scan()
    -- สแกนทุก 0.5 วิ (ประหยัด FPS)
    if tick() - self.LastScan < 0.5 then return end
    self.LastScan = tick()
    
    for _, player in ipairs(Players:GetPlayers()) do
        if player ~= LocalPlayer and player.Character then
            self:ProcessCharacter(player.Character)
        end
    end
end

function Hitbox:Toggle()
    self.Enabled = not self.Enabled
    
    if self.Enabled then
        print("[Hitbox] เปิดใช้งาน (x" .. self.Size .. ")")
        -- ประมวลผลทันที
        for _, player in ipairs(Players:GetPlayers()) do
            if player ~= LocalPlayer and player.Character then
                self:ProcessCharacter(player.Character)
            end
        end
        
        -- Loop ตรวจสอบผู้เล่นใหม่
        self.Connection = game:GetService("RunService").Heartbeat:Connect(function()
            self:Scan()
        end)
    else
        print("[Hitbox] ปิดใช้งาน - คืนค่า")
        if self.Connection then
            self.Connection:Disconnect()
            self.Connection = nil
        end
        
        -- คืนค่าทั้งหมด
        for part, data in pairs(self.Originals) do
            if part and part.Parent then
                part.Size = data.Size
                part.Transparency = data.Transparency
                part.Material = data.Material
            end
        end
        self.Originals = {}
    end
    
    return self.Enabled
end

-- UI (LayoutOrder 14)
local HitFrame = Instance.new("Frame")
HitFrame.Size = UDim2.new(1, -30, 0, 120)
HitFrame.BackgroundColor3 = Color3.fromRGB(35, 35, 48)
HitFrame.BorderSizePixel = 0
HitFrame.LayoutOrder = 14
HitFrame.Parent = MainPage

local HitStroke = Instance.new("UIStroke", HitFrame)
HitStroke.Color = Color3.fromRGB(70, 70, 120)
HitStroke.Thickness = 1

local HitTitle = Instance.new("TextLabel")
HitTitle.Size = UDim2.new(1, -20, 0, 26)
HitTitle.Position = UDim2.new(0, 10, 0, 8)
HitTitle.BackgroundTransparency = 1
HitTitle.Text = "📦 HITBOX EXPANDER"
HitTitle.TextColor3 = Color3.fromRGB(255, 100, 100)
HitTitle.Font = Enum.Font.GothamBold
HitTitle.TextSize = 15
HitTitle.TextXAlignment = Enum.TextXAlignment.Left
HitTitle.Parent = HitFrame

local HitSub = Instance.new("TextLabel")
HitSub.Size = UDim2.new(1, -20, 0, 20)
HitSub.Position = UDim2.new(0, 10, 0, 32)
HitSub.BackgroundTransparency = 1
HitSub.Text = "ขยายส่วนหัว/ตัวของศัตรูให้โดนง่าย"
HitSub.TextColor3 = Color3.fromRGB(180, 180, 220)
HitSub.Font = Enum.Font.Gotham
HitSub.TextSize = 11
HitSub.TextXAlignment = Enum.TextXAlignment.Left
HitSub.Parent = HitFrame

local HitBtn = Instance.new("TextButton")
HitBtn.Size = UDim2.new(0.5, -5, 0, 36)
HitBtn.Position = UDim2.new(0, 10, 0, 60)
HitBtn.BackgroundColor3 = Color3.fromRGB(180, 60, 60)
HitBtn.Text = "OFF"
HitBtn.TextColor3 = Color3.new(1, 1, 1)
HitBtn.Font = Enum.Font.GothamBold
HitBtn.TextSize = 14
HitBtn.BorderSizePixel = 0
HitBtn.Parent = HitFrame

local SizeBtn = Instance.new("TextButton")
SizeBtn.Size = UDim2.new(0.45, 0, 0, 36)
SizeBtn.Position = UDim2.new(0.55, 0, 0, 60)
SizeBtn.BackgroundColor3 = Color3.fromRGB(80, 80, 140)
SizeBtn.Text = "Size: x3"
SizeBtn.TextColor3 = Color3.new(1, 1, 1)
SizeBtn.Font = Enum.Font.Gotham
SizeBtn.TextSize = 12
SizeBtn.BorderSizePixel = 0
SizeBtn.Parent = HitFrame

local Status = Instance.new("TextLabel")
Status.Size = UDim2.new(1, -20, 0, 20)
Status.Position = UDim2.new(0, 10, 0, 100)
Status.BackgroundTransparency = 1
Status.Text = "เป้าหมาย: ศัตรูในทีมอื่น"
Status.TextColor3 = Color3.fromRGB(150, 150, 180)
Status.Font = Enum.Font.Gotham
Status.TextSize = 11
Status.TextXAlignment = Enum.TextXAlignment.Left
Status.Parent = HitFrame

-- Hover Effects
HitBtn.MouseEnter:Connect(function()
    local color = Hitbox.Enabled and Color3.fromRGB(80, 200, 100) or Color3.fromRGB(200, 80, 80)
    TweenService:Create(HitBtn, TweenInfo.new(0.15), {BackgroundColor3 = color}):Play()
end)
HitBtn.MouseLeave:Connect(function()
    local color = Hitbox.Enabled and Color3.fromRGB(60, 160, 80) or Color3.fromRGB(180, 60, 60)
    TweenService:Create(HitBtn, TweenInfo.new(0.15), {BackgroundColor3 = color}):Play()
end)

-- กด Toggle
HitBtn.MouseButton1Click:Connect(function()
    local state = Hitbox:Toggle()
    if state then
        HitBtn.Text = "ON"
        HitBtn.BackgroundColor3 = Color3.fromRGB(60, 160, 80)
        HitTitle.TextColor3 = Color3.fromRGB(150, 255, 150)
        Status.Text = "กำลังขยาย Hitbox ศัตรู..."
        Status.TextColor3 = Color3.fromRGB(100, 255, 100)
    else
        HitBtn.Text = "OFF"
        HitBtn.BackgroundColor3 = Color3.fromRGB(180, 60, 60)
        HitTitle.TextColor3 = Color3.fromRGB(255, 100, 100)
        Status.Text = "เป้าหมาย: ศัตรูในทีมอื่น"
        Status.TextColor3 = Color3.fromRGB(150, 150, 180)
    end
end)

-- เปลี่ยนขนาด
SizeBtn.MouseButton1Click:Connect(function()
    local sizes = {2, 3, 5, 10}
    local idx = table.find(sizes, Hitbox.Size) or 1
    idx = idx % #sizes + 1
    Hitbox.Size = sizes[idx]
    SizeBtn.Text = "Size: x" .. Hitbox.Size
    
    -- รีเฟรชถ้ากำลังทำงาน
    if Hitbox.Enabled then
        for _, player in ipairs(Players:GetPlayers()) do
            if player ~= LocalPlayer and player.Character then
                Hitbox:ProcessCharacter(player.Character)
            end
        end
    end
end)

SizeBtn.MouseEnter:Connect(function()
    TweenService:Create(SizeBtn, TweenInfo.new(0.15), {BackgroundColor3 = Color3.fromRGB(100, 100, 170)}):Play()
end)
SizeBtn.MouseLeave:Connect(function()
    TweenService:Create(SizeBtn, TweenInfo.new(0.15), {BackgroundColor3 = Color3.fromRGB(80, 80, 140)}):Play()
end)

-- ติดตามผู้เล่นใหม่
Players.PlayerAdded:Connect(function(player)
    player.CharacterAdded:Connect(function(char)
        if Hitbox.Enabled then
            task.wait(1)
            Hitbox:ProcessCharacter(char)
        end
    end)
end)

-- Cleanup
sg.Destroying:Connect(function()
    Hitbox.Enabled = false
    if Hitbox.Connection then Hitbox.Connection:Disconnect() end
    -- คืนค่าทั้งหมด
    for part, data in pairs(Hitbox.Originals) do
        if part and part.Parent then
            part.Size = data.Size
            part.Transparency = data.Transparency
            part.Material = data.Material
        end
    end
end)

print("[Hitbox] Hitbox Expander Loaded (Layout 14)")
print("[Hitbox] ขยาย Head/HRP x3 (default), สแกนทุก 0.5s")
-- ==============================================
-- FLY MODE - ระบบบิน (Layout 15)
-- ==============================================

local FlySystem = {
    Loaded = false,
    Gui = nil,
    Connection = nil
}

function FlySystem:Unload()
    if self.Gui then
        pcall(function()
            self.Gui:Destroy()
        end)
        self.Gui = nil
    end
    
    if self.Connection then
        self.Connection:Disconnect()
        self.Connection = nil
    end
    
    self.Loaded = false
    print("[Fly] รีเซ็ตสถานะเรียบร้อย")
end

function FlySystem:Load()
    if self.Loaded then
        print("[Fly] โหลดแล้วก่อนหน้านี้ กำลังรีเซ็ต...")
        self:Unload()
    end
    
    print("[Fly] กำลังโหลด Fly Gui V3...")
    local success, err = pcall(function()
        self.Gui = loadstring(game:HttpGet("https://raw.githubusercontent.com/XNEOFF/FlyGuiV3/main/FlyGuiV3.txt"))()
    end)
    
    if success then
        self.Loaded = true
        print("[Fly] โหลดสำเร็จ! (กดปุ่มใน GUI ที่โหลดขึ้นมาเพื่อบิน)")
        return true
    else
        warn("[Fly] โหลดไม่สำเร็จ:", err)
        self.Loaded = false
        return false, err
    end
end

-- UI (LayoutOrder 15)
local FlyFrame = Instance.new("Frame")
FlyFrame.Size = UDim2.new(1, -30, 0, 90)
FlyFrame.BackgroundColor3 = Color3.fromRGB(35, 35, 48)
FlyFrame.BorderSizePixel = 0
FlyFrame.LayoutOrder = 15
FlyFrame.Parent = MainPage

local FlyStroke = Instance.new("UIStroke", FlyFrame)
FlyStroke.Color = Color3.fromRGB(70, 70, 120)
FlyStroke.Thickness = 1

local FlyTitle = Instance.new("TextLabel")
FlyTitle.Size = UDim2.new(1, -20, 0, 26)
FlyTitle.Position = UDim2.new(0, 10, 0, 8)
FlyTitle.BackgroundTransparency = 1
FlyTitle.Text = "🕊️ FLY MODE"
FlyTitle.TextColor3 = Color3.fromRGB(100, 200, 255)
FlyTitle.Font = Enum.Font.GothamBold
FlyTitle.TextSize = 15
FlyTitle.TextXAlignment = Enum.TextXAlignment.Left
FlyTitle.Parent = FlyFrame

local FlySub = Instance.new("TextLabel")
FlySub.Size = UDim2.new(1, -20, 0, 20)
FlySub.Position = UDim2.new(0, 10, 0, 32)
FlySub.BackgroundTransparency = 1
FlySub.Text = "โหลดระบบบิน (FlyGuiV3)"
FlySub.TextColor3 = Color3.fromRGB(180, 180, 220)
FlySub.Font = Enum.Font.Gotham
FlySub.TextSize = 11
FlySub.TextXAlignment = Enum.TextXAlignment.Left
FlySub.Parent = FlyFrame

local LoadFlyBtn = Instance.new("TextButton")
LoadFlyBtn.Size = UDim2.new(0.9, 0, 0, 32)
LoadFlyBtn.Position = UDim2.new(0.05, 0, 0, 52)
LoadFlyBtn.BackgroundColor3 = Color3.fromRGB(80, 140, 180)
LoadFlyBtn.Text = "LOAD FLY GUI"
LoadFlyBtn.TextColor3 = Color3.new(1, 1, 1)
LoadFlyBtn.Font = Enum.Font.GothamBold
LoadFlyBtn.TextSize = 14
LoadFlyBtn.BorderSizePixel = 0
LoadFlyBtn.Parent = FlyFrame

-- Hover Effects
LoadFlyBtn.MouseEnter:Connect(function()
    if not FlySystem.Loaded then
        TweenService:Create(LoadFlyBtn, TweenInfo.new(0.15), {BackgroundColor3 = Color3.fromRGB(100, 170, 200)}):Play()
    end
end)

LoadFlyBtn.MouseLeave:Connect(function()
    local color = FlySystem.Loaded and Color3.fromRGB(60, 160, 80) or Color3.fromRGB(80, 140, 180)
    TweenService:Create(LoadFlyBtn, TweenInfo.new(0.15), {BackgroundColor3 = color}):Play()
end)

-- กดโหลด
LoadFlyBtn.MouseButton1Click:Connect(function()
    if FlySystem.Loaded then
        LoadFlyBtn.Text = "UNLOADING..."
        LoadFlyBtn.BackgroundColor3 = Color3.fromRGB(100, 100, 100)
        
        FlySystem:Unload()
        
        LoadFlyBtn.Text = "LOAD FLY GUI"
        LoadFlyBtn.BackgroundColor3 = Color3.fromRGB(80, 140, 180)
        FlyTitle.TextColor3 = Color3.fromRGB(100, 200, 255)
        FlySub.Text = "โหลดระบบบิน (FlyGuiV3)"
        return
    end
    
    LoadFlyBtn.Text = "LOADING..."
    LoadFlyBtn.BackgroundColor3 = Color3.fromRGB(100, 100, 100)
    
    local success, err = FlySystem:Load()
    
    if success then
        LoadFlyBtn.Text = "LOADED ✓"
        LoadFlyBtn.BackgroundColor3 = Color3.fromRGB(60, 160, 80)
        FlyTitle.TextColor3 = Color3.fromRGB(150, 255, 150)
        FlySub.Text = "พร้อมใช้งาน - กดปุ่มใน GUI"
    else
        LoadFlyBtn.Text = "ERROR! Retry"
        LoadFlyBtn.BackgroundColor3 = Color3.fromRGB(180, 60, 60)
        FlySub.Text = "Error: " .. tostring(err):sub(1, 30)
        
        task.wait(2)
        LoadFlyBtn.Text = "LOAD FLY GUI"
        LoadFlyBtn.BackgroundColor3 = Color3.fromRGB(80, 140, 180)
        FlySub.Text = "โหลดระบบบิน (FlyGuiV3)"
    end
end)

print("[Fly] Fly Mode Button Ready (Layout 15)")
print("[Fly] กด LOAD เพื่อโหลด FlyGuiV3 จาก GitHub")
-- ==============================================
-- FLOAT / ANTI-GRAVITY - ลอยจากพื้น (Layout 16)
-- ==============================================

local Float = {
    Enabled = false,
    Height = 3, -- ความสูงจากพื้น (studs)
    Connection = nil
}

function Float:Apply()
    if not self.Enabled then return end
    local char = LocalPlayer.Character
    if not char then return end
    
    local hrp = char:FindFirstChild("HumanoidRootPart")
    local hum = char:FindFirstChildOfClass("Humanoid")
    if not hrp or not hum then return end
    
    -- Raycast หาพื้น
    local rayOrigin = hrp.Position
    local rayDir = Vector3.new(0, -100, 0)
    local rayParams = RaycastParams.new()
    rayParams.FilterDescendantsInstances = {char}
    rayParams.FilterType = Enum.RaycastFilterType.Blacklist
    
    local result = workspace:Raycast(rayOrigin, rayDir, rayParams)
    if result then
        local groundY = result.Position.Y
        local targetY = groundY + self.Height
        local currentY = hrp.Position.Y
        
        -- ถ้าต่ำกว่าความสูงที่ต้องการ ให้ผลักขึ้น
        if currentY < targetY then
            local vel = hrp.AssemblyLinearVelocity
            hrp.AssemblyLinearVelocity = Vector3.new(vel.X, 25, vel.Z)
            hum.PlatformStand = true
        else
            hum.PlatformStand = false
        end
    end
end

function Float:Toggle()
    self.Enabled = not self.Enabled
    
    if self.Enabled then
        print("[Float] เปิดใช้งาน (สูง " .. self.Height .. " studs)")
        self.Connection = game:GetService("RunService").Heartbeat:Connect(function()
            self:Apply()
        end)
    else
        print("[Float] ปิดใช้งาน")
        if self.Connection then
            self.Connection:Disconnect()
            self.Connection = nil
        end
        local hum = LocalPlayer.Character and LocalPlayer.Character:FindFirstChildOfClass("Humanoid")
        if hum then hum.PlatformStand = false end
    end
    
    return self.Enabled
end

-- UI (LayoutOrder 16)
local FloatFrame = Instance.new("Frame")
FloatFrame.Size = UDim2.new(1, -30, 0, 110)
FloatFrame.BackgroundColor3 = Color3.fromRGB(35, 35, 48)
FloatFrame.BorderSizePixel = 0
FloatFrame.LayoutOrder = 16
FloatFrame.Parent = MainPage

local FloatStroke = Instance.new("UIStroke", FloatFrame)
FloatStroke.Color = Color3.fromRGB(70, 70, 120)
FloatStroke.Thickness = 1

local FloatTitle = Instance.new("TextLabel")
FloatTitle.Size = UDim2.new(1, -20, 0, 26)
FloatTitle.Position = UDim2.new(0, 10, 0, 8)
FloatTitle.BackgroundTransparency = 1
FloatTitle.Text = "☁️ FLOAT / ANTI-GRAVITY"
FloatTitle.TextColor3 = Color3.fromRGB(100, 200, 255)
FloatTitle.Font = Enum.Font.GothamBold
FloatTitle.TextSize = 15
FloatTitle.TextXAlignment = Enum.TextXAlignment.Left
FloatTitle.Parent = FloatFrame

local FloatSub = Instance.new("TextLabel")
FloatSub.Size = UDim2.new(1, -20, 0, 20)
FloatSub.Position = UDim2.new(0, 10, 0, 32)
FloatSub.BackgroundTransparency = 1
FloatSub.Text = "ลอยขึ้นจากพื้นเพื่อหลบการโจมตี"
FloatSub.TextColor3 = Color3.fromRGB(180, 180, 220)
FloatSub.Font = Enum.Font.Gotham
FloatSub.TextSize = 11
FloatSub.TextXAlignment = Enum.TextXAlignment.Left
FloatSub.Parent = FloatFrame

local FloatBtn = Instance.new("TextButton")
FloatBtn.Size = UDim2.new(0.5, -5, 0, 36)
FloatBtn.Position = UDim2.new(0, 10, 0, 58)
FloatBtn.BackgroundColor3 = Color3.fromRGB(180, 60, 60)
FloatBtn.Text = "OFF"
FloatBtn.TextColor3 = Color3.new(1, 1, 1)
FloatBtn.Font = Enum.Font.GothamBold
FloatBtn.TextSize = 14
FloatBtn.BorderSizePixel = 0
FloatBtn.Parent = FloatFrame

local HeightBtn = Instance.new("TextButton")
HeightBtn.Size = UDim2.new(0.45, 0, 0, 36)
HeightBtn.Position = UDim2.new(0.55, 0, 0, 58)
HeightBtn.BackgroundColor3 = Color3.fromRGB(80, 80, 140)
HeightBtn.Text = "Height: 3"
HeightBtn.TextColor3 = Color3.new(1, 1, 1)
HeightBtn.Font = Enum.Font.Gotham
HeightBtn.TextSize = 12
HeightBtn.BorderSizePixel = 0
HeightBtn.Parent = FloatFrame

-- Hover Effects
FloatBtn.MouseEnter:Connect(function()
    local color = Float.Enabled and Color3.fromRGB(80, 200, 100) or Color3.fromRGB(200, 80, 80)
    TweenService:Create(FloatBtn, TweenInfo.new(0.15), {BackgroundColor3 = color}):Play()
end)
FloatBtn.MouseLeave:Connect(function()
    local color = Float.Enabled and Color3.fromRGB(60, 160, 80) or Color3.fromRGB(180, 60, 60)
    TweenService:Create(FloatBtn, TweenInfo.new(0.15), {BackgroundColor3 = color}):Play()
end)

-- กด Toggle
FloatBtn.MouseButton1Click:Connect(function()
    local state = Float:Toggle()
    if state then
        FloatBtn.Text = "ON"
        FloatBtn.BackgroundColor3 = Color3.fromRGB(60, 160, 80)
        FloatTitle.TextColor3 = Color3.fromRGB(150, 255, 150)
    else
        FloatBtn.Text = "OFF"
        FloatBtn.BackgroundColor3 = Color3.fromRGB(180, 60, 60)
        FloatTitle.TextColor3 = Color3.fromRGB(100, 200, 255)
    end
end)

-- เปลี่ยนความสูง (1-5 studs)
HeightBtn.MouseButton1Click:Connect(function()
    Float.Height = Float.Height % 5 + 1
    HeightBtn.Text = "Height: " .. Float.Height
end)

HeightBtn.MouseEnter:Connect(function()
    TweenService:Create(HeightBtn, TweenInfo.new(0.15), {BackgroundColor3 = Color3.fromRGB(100, 100, 170)}):Play()
end)
HeightBtn.MouseLeave:Connect(function()
    TweenService:Create(HeightBtn, TweenInfo.new(0.15), {BackgroundColor3 = Color3.fromRGB(80, 80, 140)}):Play()
end)

-- Cleanup
sg.Destroying:Connect(function()
    Float.Enabled = false
    if Float.Connection then Float.Connection:Disconnect() end
    local hum = LocalPlayer.Character and LocalPlayer.Character:FindFirstChildOfClass("Humanoid")
    if hum then hum.PlatformStand = false end
end)

print("[Float] Float / Anti-Gravity Loaded (Layout 16)")
print("[Float] ปรับความสูง 1-5 studs, กดปุ่ม ON เพื่อลอย")
-- ==============================================
-- AUTO INTERACT - เก็บของ/กดปุ่มอัตโนมัติ (Universal)
-- ==============================================

local AutoInteract = {
    Enabled = false,
    Range = 15,
    LastUpdate = 0,
    Connection = nil
}

function AutoInteract:Toggle()
    self.Enabled = not self.Enabled
    
    if self.Enabled then
        print("[AutoInteract] เปิดใช้งาน")
        -- Loop ตรวจสอบ ProximityPrompt และ ClickDetector
        self.Connection = game:GetService("RunService").Heartbeat:Connect(function()
            if tick() - self.LastUpdate >= 0.2 then -- ตรวจสอบทุก 0.2 วินาทีเพื่อไม่ให้แลค
                self.LastUpdate = tick()
                
                local char = LocalPlayer.Character
                local hrp = char and char:FindFirstChild("HumanoidRootPart")
                if not hrp then return end
                
                -- ค้นหา interaction ในรัศมี
                for _, v in ipairs(workspace:GetDescendants()) do
                    if v:IsA("ProximityPrompt") or v:IsA("ClickDetector") then
                        local target = v.Parent:IsA("Model") and (v.Parent.PrimaryPart or v.Parent:FindFirstChildWhichIsA("BasePart")) or v.Parent
                        
                        if target and target:IsA("BasePart") then
                            local dist = (target.Position - hrp.Position).Magnitude
                            if dist <= self.Range then
                                if v:IsA("ProximityPrompt") then
                                    v:InputHoldBegin()
                                    task.wait()
                                    v:InputHoldEnd()
                                else
                                    fireclickdetector(v)
                                end
                            end
                        end
                    end
                end
            end
        end)
    else
        print("[AutoInteract] ปิดใช้งาน")
        if self.Connection then
            self.Connection:Disconnect()
            self.Connection = nil
        end
    end
    return self.Enabled
end
-- UI สำหรับ Auto Interact (ดีไซน์เดียวกับ Speed/Jump Frame)
local AutoIntFrame = Instance.new("Frame")
AutoIntFrame.Size = UDim2.new(1, -30, 0, 90) -- ขนาดกะทัดรัด
AutoIntFrame.BackgroundColor3 = Color3.fromRGB(35, 35, 48)
AutoIntFrame.BorderSizePixel = 0
AutoIntFrame.LayoutOrder = 17 -- ต่อจาก Float
AutoIntFrame.Parent = MainPage

local AutoIntStroke = Instance.new("UIStroke", AutoIntFrame)
AutoIntStroke.Color = Color3.fromRGB(70, 70, 120)
AutoIntStroke.Thickness = 1

local AutoIntTitle = Instance.new("TextLabel")
AutoIntTitle.Size = UDim2.new(1, -20, 0, 24)
AutoIntTitle.Position = UDim2.new(0, 10, 0, 8)
AutoIntTitle.BackgroundTransparency = 1
AutoIntTitle.Text = "🖐️ AUTO INTERACT"
AutoIntTitle.TextColor3 = Color3.fromRGB(150, 255, 150)
AutoIntTitle.Font = Enum.Font.GothamBold
AutoIntTitle.TextSize = 15
AutoIntTitle.TextXAlignment = Enum.TextXAlignment.Left
AutoIntTitle.Parent = AutoIntFrame

-- ปุ่มเปิด/ปิด
local AutoIntBtn = Instance.new("TextButton")
AutoIntBtn.Size = UDim2.new(0.9, 0, 0, 35)
AutoIntBtn.Position = UDim2.new(0.05, 0, 0, 42)
AutoIntBtn.BackgroundColor3 = Color3.fromRGB(180, 60, 60)
AutoIntBtn.Text = "OFF"
AutoIntBtn.TextColor3 = Color3.new(1, 1, 1)
AutoIntBtn.Font = Enum.Font.GothamBold
AutoIntBtn.TextSize = 14
AutoIntBtn.BorderSizePixel = 0
AutoIntBtn.Parent = AutoIntFrame

AutoIntBtn.MouseEnter:Connect(function()
    local color = AutoInteract.Enabled and Color3.fromRGB(80, 200, 100) or Color3.fromRGB(200, 80, 80)
    TweenService:Create(AutoIntBtn, TweenInfo.new(0.15), {BackgroundColor3 = color}):Play()
end)

AutoIntBtn.MouseLeave:Connect(function()
    local color = AutoInteract.Enabled and Color3.fromRGB(60, 160, 80) or Color3.fromRGB(180, 60, 60)
    TweenService:Create(AutoIntBtn, TweenInfo.new(0.15), {BackgroundColor3 = color}):Play()
end)

-- ฟังก์ชันกดปุ่ม
AutoIntBtn.MouseButton1Click:Connect(function()
    local state = AutoInteract:Toggle()
    if state then
        AutoIntBtn.Text = "ON"
        AutoIntBtn.BackgroundColor3 = Color3.fromRGB(60, 160, 80)
    else
        AutoIntBtn.Text = "OFF"
        AutoIntBtn.BackgroundColor3 = Color3.fromRGB(180, 60, 60)
    end
end)

-- ==============================================
-- แท็บ Settings - ตั้งค่า GUI (ไม่มีมุมโค้งแล้ว)
-- ==============================================

local guiSettings = {
    bgColor = Color3.fromRGB(20, 20, 28),
    titleColor = Color3.fromRGB(28, 28, 38),
    transparency = 0,
    textSizeScale = 1.0,
    strokeThickness = 1
}

local function updateGUI()
    TweenService:Create(Main, TweenInfo.new(0.4), {
        BackgroundColor3 = guiSettings.bgColor,
        BackgroundTransparency = guiSettings.transparency
    }):Play()
    
    TweenService:Create(Title, TweenInfo.new(0.4), {
        BackgroundColor3 = guiSettings.titleColor
    }):Play()
    
    Stroke.Thickness = guiSettings.strokeThickness
    
    -- อัพเดท text size scale
    for _, obj in ipairs(Main:GetDescendants()) do
        if obj:IsA("TextLabel") or obj:IsA("TextButton") or obj:IsA("TextBox") then
            if obj ~= TitleText then
                obj.TextSize = obj.TextSize * guiSettings.textSizeScale
            end
        end
    end
end

local function addSettingRow(labelText, defaultValue, callback)
    local row = Instance.new("Frame")
    row.Size = UDim2.new(1, -30, 0, 38)
    row.BackgroundTransparency = 1
    row.Parent = SettingsPage

    local lbl = Instance.new("TextLabel")
    lbl.Size = UDim2.new(0.55, 0, 1, 0)
    lbl.BackgroundTransparency = 1
    lbl.Text = labelText
    lbl.TextColor3 = Color3.fromRGB(180, 180, 220)
    lbl.Font = Enum.Font.GothamMedium
    lbl.TextSize = 14
    lbl.TextXAlignment = Enum.TextXAlignment.Left
    lbl.Parent = row

    local input = Instance.new("TextBox")
    input.Size = UDim2.new(0.45, 0, 0.8, 0)
    input.Position = UDim2.new(0.55, 0, 0.1, 0)
    input.BackgroundColor3 = Color3.fromRGB(40, 40, 55)
    input.TextColor3 = Color3.new(1,1,1)
    input.Text = tostring(defaultValue)
    input.Font = Enum.Font.Gotham
    input.TextSize = 13
    input.ClearTextOnFocus = false
    input.Parent = row

    -- ไม่มี UICorner → มุมเหลี่ยม

    input.FocusLost:Connect(function(enter)
        if enter then
            callback(input.Text)
            updateGUI()
        end
    end)
end

-- การตั้งค่า (ไม่มี Corner Radius แล้ว)
addSettingRow("สีพื้นหลัง (R,G,B)", "20,20,28", function(val)
    local r,g,b = val:match("(%d+),%s*(%d+),%s*(%d+)")
    if r and g and b then guiSettings.bgColor = Color3.fromRGB(tonumber(r),tonumber(g),tonumber(b)) end
end)

addSettingRow("สี Title Bar (R,G,B)", "28,28,38", function(val)
    local r,g,b = val:match("(%d+),%s*(%d+),%s*(%d+)")
    if r and g and b then guiSettings.titleColor = Color3.fromRGB(tonumber(r),tonumber(g),tonumber(b)) end
end)

addSettingRow("ความโปร่งใส (0-1)", "0", function(val)
    local n = tonumber(val)
    if n and n >= 0 and n <= 1 then guiSettings.transparency = n end
end)

addSettingRow("ขนาดตัวอักษร Scale (0.8-1.5)", "1.0", function(val)
    local n = tonumber(val)
    if n and n >= 0.8 and n <= 1.5 then guiSettings.textSizeScale = n end
end)

addSettingRow("ความหนาขอบ (0.5-3)", "1", function(val)
    local n = tonumber(val)
    if n and n >= 0.5 and n <= 3 then guiSettings.strokeThickness = n end
end)

-- Reset Button
local resetBtn = Instance.new("TextButton")
resetBtn.Size = UDim2.new(1, -40, 0, 45)
resetBtn.BackgroundColor3 = Color3.fromRGB(100, 100, 160)
resetBtn.Text = "Reset to Default"
resetBtn.TextColor3 = Color3.new(1,1,1)
resetBtn.Font = Enum.Font.GothamBold
resetBtn.TextSize = 16
resetBtn.BorderSizePixel = 0
resetBtn.Parent = SettingsPage

resetBtn.MouseButton1Click:Connect(function()
    guiSettings = {
        bgColor = Color3.fromRGB(20, 20, 28),
        titleColor = Color3.fromRGB(28, 28, 38),
        transparency = 0,
        textSizeScale = 1.0,
        strokeThickness = 1
    }
    updateGUI()
    print("Reset GUI settings to default")
end)

-- ============================================================
-- สิ้นสุดส่วน Settings
-- ============================================================

-- Maximize (ไม่ใหญ่เกิน)
local isMax = false
local normalSize = Main.Size
local normalPos = Main.Position

MaxBtn.MouseButton1Click:Connect(function()
    isMax = not isMax
    if isMax then
        TweenService:Create(Main, TweenInfo.new(0.3, Enum.EasingStyle.Quad), {
            Size = UDim2.new(0.92, 0, 0.88, 0),
            Position = UDim2.new(0.04, 0, 0.06, 0)
        }):Play()
        MaxBtn.Text = "❐"
    else
        TweenService:Create(Main, TweenInfo.new(0.3, Enum.EasingStyle.Quad), {
            Size = normalSize,
            Position = normalPos
        }):Play()
        MaxBtn.Text = "□"
    end
end)

-- Minimize + Icon
local minIcon = Instance.new("TextButton")
minIcon.Size = UDim2.new(0, 48, 0, 48)
minIcon.Position = UDim2.new(0, 15, 1, -65)
minIcon.BackgroundColor3 = Color3.fromRGB(40, 40, 60)
minIcon.Text = "Y"
minIcon.TextColor3 = Color3.fromRGB(180, 180, 255)
minIcon.Font = Enum.Font.GothamBold
minIcon.TextSize = 18
minIcon.BorderSizePixel = 0
minIcon.Visible = false
minIcon.Parent = sg

MinBtn.MouseButton1Click:Connect(function()
    TweenService:Create(Main, TweenInfo.new(0.22, Enum.EasingStyle.Quad), {
        Size = UDim2.new(0,0,0,0),
        Position = UDim2.new(0.5,0,0.5,0),
        BackgroundTransparency = 1
    }):Play()
    task.delay(0.22, function()
        Main.Visible = false
        minIcon.Visible = true
    end)
end)

minIcon.MouseButton1Click:Connect(function()
    Main.Visible = true
    Main.BackgroundTransparency = 1
    Main.Size = UDim2.new(0,0,0,0)
    Main.Position = UDim2.new(0.5,0,0.5,0)
    TweenService:Create(Main, TweenInfo.new(0.3, Enum.EasingStyle.Quad), {
        Size = normalSize,
        Position = normalPos,
        BackgroundTransparency = 0
    }):Play()
    minIcon.Visible = false
end)

-- Close
CloseBtn.MouseButton1Click:Connect(function()
    TweenService:Create(Main, TweenInfo.new(0.2, Enum.EasingStyle.Quad), {
        BackgroundTransparency = 1,
        Size = UDim2.new(0.75,0,0.75,0)
    }):Play()
    task.delay(0.2, function() sg:Destroy() end)
end)

-- Draggable
local dragging, dragStart, startPos
Title.InputBegan:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
        dragging = true
        dragStart = input.Position
        startPos = Main.Position
    end
end)

Title.InputEnded:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
        dragging = false
    end
end)

UserInputService.InputChanged:Connect(function(input)
    if dragging and (input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch) then
        local delta = input.Position - dragStart
        Main.Position = UDim2.new(startPos.X.Scale, startPos.X.Offset + delta.X, startPos.Y.Scale, startPos.Y.Offset + delta.Y)
    end
end)

-- Open Animation
Main.BackgroundTransparency = 1
Main.Size = UDim2.new(0.88, 0, 0.88, 0)
Main.Position = UDim2.new(0.5, -Main.AbsoluteSize.X*0.44, 0.5, -Main.AbsoluteSize.Y*0.44)
Main.Visible = true

TweenService:Create(Main, TweenInfo.new(0.35, Enum.EasingStyle.Quad), {
    BackgroundTransparency = 0,
    Size = UDim2.new(0, 400, 0, 340),
    Position = UDim2.new(0.5, -200, 0.5, -170)
}):Play()

print("Hub v12.1 Loaded – มุมเหลี่ยมหมดทุกจุดแล้ว")
-- ==============================================
-- ANTI-BAN SYSTEM v2.0 - ป้องกันแบน/เตะ/ตรวจจับ
-- แก้ไขสำหรับ Delta Executor (ลบโค้ดซ้ำ + ใช้ pcall)
-- ==============================================

local AntiBan = {
    Enabled = true,
    Initialized = false,
    LastCheck = tick(),
    CheckInterval = 10,
    SuspiciousActivity = 0,
    
    Settings = {
        AntiKick = true,
        AntiBan = true,
        AntiDetection = true
    }
}

-- ป้องกันการโดนเตะ
function AntiBan:AntiKick()
    local Players = game:GetService("Players")
    local LocalPlayer = Players.LocalPlayer
    
    Players.ChildRemoved:Connect(function(child)
        if child == LocalPlayer and self.Settings.AntiKick then
            task.spawn(function()
                task.wait(0.5)
                if not LocalPlayer.Parent then
                    print("[AntiBan] ⚠️ Kick detected")
                end
            end)
        end
    end)
end

-- ป้องกันการแบน
function AntiBan:AntiBan()
    game.DescendantAdded:Connect(function(descendant)
        if descendant:IsA("Script") or descendant:IsA("LocalScript") then
            local name = descendant.Name:lower()
            if name:find("ban") or name:find("anticheat") or name:find("security") then
                print("[AntiBan] ⚠️ Detected:", descendant.Name)
            end
        end
    end)
end

-- ป้องกันการตรวจจับ
function AntiBan:AntiRobloxDetection()
    local CoreGui = game:GetService("CoreGui")
    
    local function checkScriptAccess(script)
        if script and script.Parent then
            local parentName = script.Parent.Name:lower()
            if parentName:find("roblox") or parentName:find("core") then
                return true
            end
        end
        return false
    end
    
    game.DescendantAdded:Connect(function(descendant)
        if descendant:IsA("Script") or descendant:IsA("LocalScript") then
            if checkScriptAccess(descendant) then
                print("[AntiBan] ⚠️ Script scan detected")
            end
        end
    end)
end

-- สุ่มดีเลย์
function AntiBan:GetRandomDelay(min, max)
    return math.random((min or 0.1) * 100, (max or 0.5) * 100) / 100
end

-- ตรวจสอบระบบ
function AntiBan:HealthCheck()
    while self.Enabled do
        task.wait(self.CheckInterval)
        if not self.Enabled then break end
        
        -- ใช้ pcall เพื่อป้องกัน error ถ้า Delta บล็อก gcinfo
        pcall(function()
            if gcinfo and gcinfo() > 100000 then
                collectgarbage("collect")
            end
        end)
    end
end

-- เปิดใช้งาน
function AntiBan:Enable()
    if self.Initialized then return end
    self.Initialized = true
    self.Enabled = true
    
    if self.Settings.AntiKick then self:AntiKick() end
    if self.Settings.AntiBan then self:AntiBan() end
    if self.Settings.AntiDetection then self:AntiRobloxDetection() end
    
    task.spawn(function()
        self:HealthCheck()
    end)
    
    print("[AntiBan] ✅ ระบบป้องกันเปิดใช้งาน (Delta Compatible)")
end

-- ปิดใช้งาน
function AntiBan:Disable()
    self.Enabled = false
    self.Initialized = false
    print("[AntiBan] ⏸️ ปิดใช้งาน")
end

-- เริ่มระบบ
AntiBan:Enable()

-- Cleanup
sg.Destroying:Connect(function()
    AntiBan:Disable()
    print("[AntiBan] 🗑️ ล้างทรัพยากร")
end)

print("[AntiBan] 🚀 Anti-Ban System Loaded (Fixed for Delta)")
